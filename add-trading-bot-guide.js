const axios = require('axios');

const DIRECTUS_URL = 'http://localhost:8055';

/**
 * 交易机器人（Trading Bot）完全指南
 */

const GUIDE_CONFIG = {
  // ===== 基本信息 =====
  title: '交易机器人（Trading Bot）完全指南',
  slug: 'trading-bot-complete-guide',
  summary:
    '从零开始搭建自动化交易机器人：涵盖网格交易、套利、做市等策略，包含 API 配置、风险管理、回测优化、监控告警全流程，适合程序员和量化交易爱好者。',

  // 在 Directus 中替换为真实分类 ID
  category: 'a6716f22-e45b-4222-989a-09f33fc2d4c5',

  // 站内一级/二级分类
  category_l1: 'tools',
  category_l2: '交易机器人',

  // ===== 元数据 =====
  difficulty_level: 4,                 // 1-5，需要编程基础
  risk_level: 4,                       // 高风险：资金、技术、市场风险
  apy_min: 0,
  apy_max: 0,

  // 资金/时间/技术门槛
  threshold_capital: '$500–5,000（初始测试资金）+ $20–50/月（服务器）',
  threshold_capital_min: 500,
  time_commitment: '初始 20–40 小时开发，每周 2–5 小时监控优化',
  time_commitment_minutes: 1200,
  threshold_tech_level: 'advanced',

  // ===== 可读性强的 Markdown 正文 =====
  content: `> **适用人群**：有编程基础（Python/JavaScript）、了解加密货币交易、追求自动化收益的量化交易者
> **阅读时间**：≈ 25–30 分钟
> **关键词**：Algorithmic Trading / Grid Bot / Arbitrage / Market Making / API / Backtesting / Risk Management / ccxt

---

## 🧭 TL;DR
- **为什么用机器人**：24/7 自动交易、情绪管理、高频执行、策略回测验证
- **核心框架**：**策略选择 → API 配置 → 代码开发 → 回测 → 实盘部署 → 监控优化**
- **硬性纪律**：**小资金测试**；**严格止损**；**API 权限最小化**；**多层监控告警**；**定期回撤分析**

---

## 🗂 目录
1. [为什么需要交易机器人？优势与风险](#为什么需要交易机器人优势与风险)
2. [五大主流策略详解](#五大主流策略详解)
3. [技术栈选择：Python vs JavaScript](#技术栈选择python-vs-javascript)
4. [三步极速上手：网格交易机器人](#三步极速上手网格交易机器人)
5. [API 安全配置与权限管理](#api-安全配置与权限管理)
6. [回测与策略优化](#回测与策略优化)
7. [监控告警系统搭建](#监控告警系统搭建)
8. [风险管理与资金控制](#风险管理与资金控制)
9. [成本收益分析](#成本收益分析)
10. [常见坑与最佳实践](#常见坑与最佳实践)
11. [FAQ](#faq)
12. [一页执行清单](#一页执行清单)

---

## 🎯 为什么需要交易机器人？优势与风险

### ✅ 核心优势

| 优势 | 说明 | 价值 |
|---|---|---|
| **24/7 运行** | 无需睡觉，抓住夜间机会 | 不错过任何行情 |
| **情绪管理** | 严格执行策略，不受 FOMO/FUD 影响 | 避免人性弱点 |
| **高频执行** | 毫秒级响应，捕捉微小价差 | 套利/做市优势 |
| **回测验证** | 用历史数据验证策略有效性 | 降低试错成本 |
| **多市场监控** | 同时监控数十个交易对 | 分散风险 + 发现机会 |

### ⚠️ 核心风险

| 风险 | 概率 | 影响 | 说明 |
|---|---|---|---|
| **技术故障** | 中 | 高 | API 断连、服务器宕机、代码 bug |
| **策略失效** | 中 | 高 | 市场环境变化导致策略不再有效 |
| **API 泄露** | 低 | 极高 | 密钥被盗，资金全部损失 |
| **滑点损失** | 高 | 中 | 高频交易累积滑点和手续费 |
| **黑天鹅** | 低 | 极高 | 极端行情导致爆仓 |

---

## 🎪 五大主流策略详解

### 1. **网格交易（Grid Trading）** ⭐⭐⭐⭐⭐

**适合场景**：震荡市场（币价在某个区间内波动）

**原理**：
- 在价格区间内设置多个买单和卖单
- 价格下跌时自动买入，上涨时自动卖出
- 通过频繁的"低买高卖"积累利润

**参数配置**：
\`\`\`python
# 示例参数
price_range = [25000, 35000]  # 价格区间（USDT）
grid_count = 20                # 网格数量
investment = 10000             # 投资金额（USDT）
\`\`\`

**收益预期**：
- 震荡市：年化 20-80%
- 单边市：可能亏损（需要止损）

**风险**：
- 单边突破：价格持续上涨或下跌时收益降低
- 手续费：频繁交易累积手续费

---

### 2. **跨交易所套利（Cross-Exchange Arbitrage）** ⭐⭐⭐⭐

**适合场景**：不同交易所价格存在差异

**原理**：
- 同时监控多个交易所的价格
- 在低价交易所买入，高价交易所卖出
- 赚取价差（扣除手续费和转账成本）

**技术要点**：
\`\`\`javascript
// 价差计算
const priceDiff = exchangeB.price - exchangeA.price;
const profit = priceDiff - (feeA + feeB + transferCost);

if (profit > minProfitThreshold) {
  // 执行套利
  await buyOnExchangeA();
  await sellOnExchangeB();
}
\`\`\`

**收益预期**：
- 年化 10-30%
- 单笔利润 0.5-2%

**风险**：
- 转账延迟：价格回归前未完成转账
- 提币限制：交易所限制提币频率
- 链上拥堵：Gas 费过高侵蚀利润

---

### 3. **做市（Market Making）** ⭐⭐⭐⭐

**适合场景**：流动性较差的交易对

**原理**：
- 同时挂买单和卖单（bid/ask）
- 赚取买卖价差（spread）
- 为市场提供流动性

**代码示例**：
\`\`\`python
import ccxt

exchange = ccxt.binance({'apiKey': 'xxx', 'secret': 'xxx'})

def place_orders(symbol, mid_price, spread, size):
    bid_price = mid_price * (1 - spread / 2)
    ask_price = mid_price * (1 + spread / 2)

    exchange.create_limit_buy_order(symbol, size, bid_price)
    exchange.create_limit_sell_order(symbol, size, ask_price)

# 每 5 秒更新订单
while True:
    mid_price = get_mid_price('BTC/USDT')
    place_orders('BTC/USDT', mid_price, spread=0.002, size=0.01)
    time.sleep(5)
\`\`\`

**收益预期**：
- 年化 15-50%
- 取决于市场波动率

**风险**：
- 库存风险：单边行情导致持仓失衡
- 竞争激烈：专业做市商挤压利润空间

---

### 4. **趋势跟随（Trend Following）** ⭐⭐⭐

**适合场景**：明确的上涨或下跌趋势

**原理**：
- 使用技术指标（MA、MACD、RSI）判断趋势
- 趋势确认后入场，趋势反转时止损

**策略示例**：
\`\`\`python
# 双均线策略
def check_signal(prices):
    ma_short = prices[-7:].mean()  # 7日均线
    ma_long = prices[-25:].mean()   # 25日均线

    if ma_short > ma_long:
        return 'BUY'
    elif ma_short < ma_long:
        return 'SELL'
    else:
        return 'HOLD'
\`\`\`

**收益预期**：
- 牛市：年化 50-200%
- 熊市：可能大幅亏损

**风险**：
- 假突破：频繁止损
- 滞后性：指标延迟导致入场晚

---

### 5. **期现套利（Spot-Futures Arbitrage）** ⭐⭐⭐⭐

**适合场景**：现货和期货价格偏离

**原理**：
- 监控现货价格和期货价格差异（基差）
- 做多现货 + 做空期货（或反向）
- 收敛时平仓获利

**收益预期**：
- 年化 15-40%
- 低风险（对冲）

**风险**：
- 资金费率：期货资金费率侵蚀利润
- 极端行情：价差持续扩大

---

## 💻 技术栈选择：Python vs JavaScript

### Python 方案 (推荐新手)

**优势**：
- 🐍 丰富的量化库（pandas、numpy、ta-lib）
- 📊 强大的数据分析能力
- 🌍 社区活跃，教程丰富

**推荐库**：
- **ccxt**：统一的交易所 API 接口
- **freqtrade**：开源交易机器人框架
- **backtrader**：回测框架

**示例代码**：
\`\`\`python
import ccxt
import pandas as pd

# 初始化交易所
exchange = ccxt.binance({
    'apiKey': 'YOUR_API_KEY',
    'secret': 'YOUR_SECRET',
    'enableRateLimit': True
})

# 获取市场数据
ohlcv = exchange.fetch_ohlcv('BTC/USDT', '1h', limit=100)
df = pd.DataFrame(ohlcv, columns=['timestamp', 'open', 'high', 'low', 'close', 'volume'])

# 计算技术指标
df['ma7'] = df['close'].rolling(7).mean()
df['ma25'] = df['close'].rolling(25).mean()

# 生成交易信号
if df['ma7'].iloc[-1] > df['ma25'].iloc[-1]:
    print("买入信号")
\`\`\`

---

### JavaScript/Node.js 方案

**优势**：
- ⚡ 异步处理能力强
- 🌐 Web 技术栈友好
- 📦 npm 生态丰富

**推荐库**：
- **ccxt**：同样支持 JS
- **tulind**：技术指标库
- **express**：搭建 API 接口

**示例代码**：
\`\`\`javascript
const ccxt = require('ccxt');

const exchange = new ccxt.binance({
  apiKey: 'YOUR_API_KEY',
  secret: 'YOUR_SECRET',
  enableRateLimit: true
});

async function placeGridOrders() {
  const ticker = await exchange.fetchTicker('BTC/USDT');
  const currentPrice = ticker.last;

  // 网格交易逻辑
  const gridLevels = 20;
  const priceStep = 500; // 每格 500 USDT

  for (let i = 1; i <= gridLevels / 2; i++) {
    const buyPrice = currentPrice - (i * priceStep);
    const sellPrice = currentPrice + (i * priceStep);

    await exchange.createLimitBuyOrder('BTC/USDT', 0.001, buyPrice);
    await exchange.createLimitSellOrder('BTC/USDT', 0.001, sellPrice);
  }
}

placeGridOrders();
\`\`\`

---

## 🚀 三步极速上手：网格交易机器人

### 第 1 步：环境准备

\`\`\`bash
# 安装 Python 和依赖
pip install ccxt pandas numpy

# 或者使用 Node.js
npm install ccxt
\`\`\`

---

### 第 2 步：编写简单网格机器人

\`\`\`python
import ccxt
import time

# 配置
SYMBOL = 'BTC/USDT'
GRID_SIZE = 10          # 网格数量
PRICE_RANGE = [28000, 32000]  # 价格区间
ORDER_SIZE = 0.001      # 每单数量

exchange = ccxt.binance({
    'apiKey': 'YOUR_API_KEY',
    'secret': 'YOUR_SECRET'
})

def create_grid_orders():
    """创建网格订单"""
    price_step = (PRICE_RANGE[1] - PRICE_RANGE[0]) / GRID_SIZE

    for i in range(GRID_SIZE):
        price = PRICE_RANGE[0] + (i * price_step)

        # 买单
        exchange.create_limit_buy_order(SYMBOL, ORDER_SIZE, price)
        print(f"买单挂单：价格 {price}")

        # 卖单
        sell_price = price + price_step
        exchange.create_limit_sell_order(SYMBOL, ORDER_SIZE, sell_price)
        print(f"卖单挂单：价格 {sell_price}")

def monitor_and_rebalance():
    """监控订单并重新平衡"""
    while True:
        orders = exchange.fetch_open_orders(SYMBOL)
        print(f"当前挂单数量：{len(orders)}")

        # 如果挂单不足，重新创建
        if len(orders) < GRID_SIZE:
            create_grid_orders()

        time.sleep(60)  # 每分钟检查一次

# 启动机器人
create_grid_orders()
monitor_and_rebalance()
\`\`\`

---

### 第 3 步：部署到云服务器

\`\`\`bash
# 使用 tmux 或 screen 保持运行
tmux new -s trading-bot
python grid_bot.py

# 分离会话
Ctrl+B, D

# 重新连接
tmux attach -t trading-bot
\`\`\`

---

## 🔐 API 安全配置与权限管理

### ⚠️ 安全规则（必须遵守！）

1. **最小权限原则**
   - ✅ 仅开启 **交易权限**（Trade）
   - ❌ 禁止 **提币权限**（Withdraw）
   - ❌ 禁止 **API 管理权限**

2. **IP 白名单**
   \`\`\`
   限制 API 只能从特定 IP 访问：
   - 云服务器 IP: 123.456.789.0
   - 本地开发 IP: 你的家庭 IP
   \`\`\`

3. **密钥存储**
   \`\`\`bash
   # 使用环境变量
   export BINANCE_API_KEY="your_key"
   export BINANCE_SECRET="your_secret"

   # 在代码中读取
   import os
   api_key = os.getenv('BINANCE_API_KEY')
   \`\`\`

4. **定期轮换密钥**
   - 每 1-3 个月更换一次 API 密钥
   - 旧密钥立即删除

---

## 📊 回测与策略优化

### 使用 Backtrader 进行回测

\`\`\`python
import backtrader as bt

class GridStrategy(bt.Strategy):
    def __init__(self):
        self.order = None

    def next(self):
        if not self.position:
            self.buy(size=1)
        elif self.data.close[0] > self.buy_price * 1.02:
            self.sell(size=1)

# 创建回测引擎
cerebro = bt.Cerebro()
cerebro.addstrategy(GridStrategy)

# 加载历史数据
data = bt.feeds.GenericCSVData(dataname='btc_usdt_1h.csv')
cerebro.adddata(data)

# 设置初始资金
cerebro.broker.setcash(10000)

# 运行回测
cerebro.run()

# 输出结果
print(f'最终资金: {cerebro.broker.getvalue()}')
cerebro.plot()
\`\`\`

### 关键回测指标

| 指标 | 说明 | 目标值 |
|---|---|---|
| **总收益率** | 策略总盈利 | > 20% |
| **夏普比率** | 风险调整收益 | > 1.5 |
| **最大回撤** | 最大亏损幅度 | < 20% |
| **胜率** | 盈利交易占比 | > 50% |
| **盈亏比** | 平均盈利/平均亏损 | > 1.5 |

---

## 📡 监控告警系统搭建

### 方案 1：Telegram Bot 告警

\`\`\`python
import requests

def send_telegram_alert(message):
    bot_token = 'YOUR_BOT_TOKEN'
    chat_id = 'YOUR_CHAT_ID'

    url = f'https://api.telegram.org/bot{bot_token}/sendMessage'
    data = {'chat_id': chat_id, 'text': message}

    requests.post(url, data=data)

# 使用示例
send_telegram_alert('⚠️ 机器人异常：API 连接失败！')
\`\`\`

### 方案 2：Prometheus + Grafana 监控

\`\`\`python
from prometheus_client import start_http_server, Gauge
import time

# 定义指标
profit_gauge = Gauge('trading_bot_profit', 'Current profit in USDT')
open_orders_gauge = Gauge('trading_bot_open_orders', 'Number of open orders')

# 启动 HTTP 服务器
start_http_server(8000)

# 更新指标
while True:
    profit = calculate_current_profit()
    profit_gauge.set(profit)

    orders = len(exchange.fetch_open_orders())
    open_orders_gauge.set(orders)

    time.sleep(60)
\`\`\`

---

## 🛡️ 风险管理与资金控制

### 1. 仓位管理

\`\`\`python
# 固定仓位法
MAX_POSITION_SIZE = 0.1  # 单个交易对最大 10% 资金

# 凯利公式
def kelly_criterion(win_rate, win_loss_ratio):
    return (win_rate * win_loss_ratio - (1 - win_rate)) / win_loss_ratio
\`\`\`

### 2. 止损策略

\`\`\`python
STOP_LOSS = 0.05  # 5% 止损

if current_loss > STOP_LOSS:
    close_all_positions()
    send_alert("触发止损，已平仓")
\`\`\`

### 3. 每日最大亏损限制

\`\`\`python
DAILY_MAX_LOSS = 200  # 每日最大亏损 $200

if daily_loss > DAILY_MAX_LOSS:
    stop_trading_for_today()
\`\`\`

---

## 💰 成本收益分析

### 成本拆解

| 项目 | 月度成本 | 年度成本 |
|---|---|---|
| **云服务器** | $30 | $360 |
| **交易手续费** (Maker 0.1%) | 变动 | ~$1,200 |
| **数据源** (可选) | $0-50 | $0-600 |
| **时间成本** | - | 200-400 小时 |

### 收益预期

**场景 A：保守网格策略**

- 初始资金：$5,000
- 年化收益：20%
- 年收益：$1,000
- **净利润**：$1,000 - $600 = **$400**

**场景 B：激进套利策略**

- 初始资金：$10,000
- 年化收益：50%
- 年收益：$5,000
- **净利润**：$5,000 - $1,200 = **$3,800**

**ROI**：38-76% (取决于策略和资金规模)

---

## 🚧 常见坑与最佳实践

### ❌ 常见错误

1. **过度优化回测**
   - 问题：参数过拟合历史数据
   - 解决：使用样本外数据验证

2. **忽略滑点和手续费**
   - 问题：回测盈利，实盘亏损
   - 解决：回测时加入真实成本

3. **没有止损**
   - 问题：单次亏损过大
   - 解决：严格执行止损规则

4. **API 密钥硬编码**
   - 问题：代码泄露导致资金损失
   - 解决：使用环境变量

5. **单点故障**
   - 问题：服务器宕机导致无法交易
   - 解决：多服务器备份 + 告警

### ✅ 最佳实践

1. **小资金测试**：先用 $100-500 测试 1-2 周
2. **逐步加仓**：验证有效后再增加资金
3. **分散策略**：同时运行 2-3 个不同策略
4. **定期复盘**：每周分析交易记录
5. **持续学习**：关注量化交易社区

---

## ❓FAQ

**Q1：没有编程基础能用机器人吗？**
> 建议先学习 Python 基础（约 1-2 个月）。或使用现成的平台（如 3Commas、Pionex）。

**Q2：机器人能保证盈利吗？**
> 不能。机器人只是工具，策略好坏决定盈亏。建议小资金测试。

**Q3：需要多少初始资金？**
> 最低 $500 测试，推荐 $2,000-5,000 正式运行。

**Q4：多久能回本？**
> 取决于策略和市场。保守策略 6-12 个月，激进策略 3-6 个月（或亏损）。

**Q5：机器人会被交易所封禁吗？**
> 正常使用不会。但需要：1) 遵守 API 速率限制，2) 不要频繁撤单。

**Q6：云服务器还是本地运行？**
> 推荐云服务器（稳定性高、延迟低）。本地运行适合测试。

---

## ✅ 一页执行清单

### 启动前
- [ ] 学习 Python/JavaScript 基础（1-2 个月）
- [ ] 了解技术指标和交易策略
- [ ] 在交易所开通 API（仅交易权限）
- [ ] 准备测试资金 $500-1,000
- [ ] 租用云服务器（推荐 DigitalOcean $30/月）

### 开发中
- [ ] 安装 ccxt 库
- [ ] 编写简单网格交易机器人
- [ ] 用历史数据回测策略
- [ ] 配置 Telegram 告警
- [ ] 设置止损和仓位管理

### 部署中
- [ ] 将代码部署到云服务器
- [ ] 配置环境变量（API 密钥）
- [ ] 使用 tmux/screen 保持运行
- [ ] 测试告警是否正常
- [ ] 小资金实盘测试（$100-500）

### 运营中
- [ ] 每天检查机器人运行状态
- [ ] 每周复盘交易记录
- [ ] 每月分析收益和回撤
- [ ] 优化策略参数
- [ ] 备份代码和配置

---

## 🎓 推荐学习资源

### 开源项目
- **freqtrade**：功能完整的交易机器人框架
- **jesse**：优雅的回测和实盘框架
- **Hummingbot**：专业做市机器人

### 教程和书籍
- **《量化交易：如何构建自己的算法交易系统》**
- **YouTube**：PartTimeLarry, Algo Trading 101
- **GitHub**：搜索 "crypto trading bot" 学习开源代码

### 社区
- **Reddit**: r/algotrading
- **Discord**: QuantConnect, Freqtrade
- **Twitter**: 关注 @cryptoquant, @glassnodealerts

---

## 📈 总结

交易机器人是 **工具而非圣杯**。成功的关键在于：

1. **扎实的编程基础**（Python/JavaScript）
2. **深入的策略理解**（不是抄代码）
3. **严格的风险管理**（止损、仓位控制）
4. **持续的学习优化**（市场在变，策略要跟上）

**记住公式**：
\`\`\`
编程能力 + 交易知识 + 风险管理 + 持续优化 = 长期稳定收益
\`\`\`

**预期 ROI**：
- 保守策略：年化 10-30%
- 激进策略：年化 30-100%（风险也更高）
- 失败概率：60-70%（大部分人因策略或风险管理失败）

🚀 **立即行动**：选择一个简单策略（推荐网格交易），写 100 行代码，小资金测试，开始你的量化交易之旅！
`,

  // ===== 与前端适配的步骤结构（5步） =====
  steps: [
    { step_number: 1, title: '策略选择与学习', description: '选择合适的交易策略（网格/套利/做市），学习 Python 或 JavaScript 基础，理解技术指标。', estimated_time: '40–80 小时' },
    { step_number: 2, title: 'API 配置与开发', description: '开通交易所 API（仅交易权限），安装 ccxt 库，编写策略代码，配置安全环境。', estimated_time: '20–40 小时' },
    { step_number: 3, title: '回测与优化', description: '使用历史数据回测策略，计算夏普比率、最大回撤等指标，优化参数配置。', estimated_time: '10–20 小时' },
    { step_number: 4, title: '实盘部署', description: '部署到云服务器，配置监控告警（Telegram/Grafana），小资金测试运行。', estimated_time: '5–10 小时' },
    { step_number: 5, title: '监控与持续优化', description: '每日检查运行状态，每周复盘交易，每月分析收益回撤，持续优化策略。', estimated_time: '2–5 小时/周' },
  ],
};

// ===== 自动执行：与既有模板一致 =====
async function getAuthToken() {
  const response = await axios.post(`${DIRECTUS_URL}/auth/login`, {
    email: 'the_uk1@outlook.com',
    password: 'Mygcdjmyxzg2026!',
  });
  return response.data.data.access_token;
}

async function addGuide() {
  try {
    const token = await getAuthToken();

    const strategy = {
      ...GUIDE_CONFIG,
      status: 'published',
      is_featured: true,
      view_count: 0,
      bookmark_count: 0,
      published_at: new Date().toISOString(),
    };

    const response = await axios.post(
      `${DIRECTUS_URL}/items/strategies`,
      strategy,
      {
        headers: {
          Authorization: `Bearer ${token}`,
          'Content-Type': 'application/json',
        },
      },
    );

    console.log('\n✅ 交易机器人完全指南创建成功!');
    console.log(`   ID: ${response.data.data.id}`);
    console.log(`   Slug: ${response.data.data.slug}`);
    console.log(`   访问: http://localhost:3000/strategies/${response.data.data.slug}\n`);
  } catch (error) {
    console.error('\n❌ 创建失败:', error.response?.data || error.message);
  }
}

addGuide();
