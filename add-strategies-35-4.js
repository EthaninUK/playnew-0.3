// 策略 35.4 & 35.5 & 35.6: 事件驱动 (event-driven-strategies)

const axios = require('axios');

/**
 * 35.4 CPI 数据公布博弈
 * 在美国 CPI 数据公布前下注，利用数据超预期或低预期的市场反应获利。
 */
const STRATEGY_35_4 = {
  title: 'CPI 数据公布博弈 - 把“通胀月度体检日”当作高波动交易窗口',
  slug: 'cpi-release-event-trade',
  summary:
    '围绕美国 CPI（月度通胀数据）公布这一固定高波动时间点，对美股指数、美元指数、黄金、比特币等资产进行短周期事件交易。核心不是盲猜数据本身，而是基于：1）市场对本次通胀的“共识预期”；2）数据实际值与预期值的偏差；3）数据结构（核心 vs 总体、环比 vs 同比）的解读差异，来寻找“预期差”驱动的方向性或波动率机会。风险极高，需要严格控制仓位与持仓时间。',
  category: 'event-driven',
  category_l1: 'derivatives',
  category_l2: 'event-driven',
  risk_level: 5,
  apy_min: 20,
  apy_max: 250,
  min_investment: 2000,
  time_commitment: '每月 2-5 小时（会前准备 + 会后执行与复盘）',
  status: 'published',
  content: `# CPI 数据公布博弈 - 把“通胀月度体检日”当作高波动交易窗口

## 💰 策略概览

| 参数 | 数值 |
|------|------|
| **适用标的** | 美股指数（标普/纳指）、美元指数、黄金、比特币等 |
| **起始资金** | 约 2,000 - 50,000 美元（合约/期权需严控杠杆） |
| **时间投入** | 每次公布前后共 2-5 小时 |
| **预期收益** | 单次事件可带来 5-30% 的权益波动（含盈亏），极端情况更大 |
| **风险等级** | ⚠️⚠️⚠️⚠️⚠️ 极高 (5/5) |
| **难度等级** | 中高级（需要对宏观预期和盘面有基本理解） |
| **适合人群** | 已经熟悉交易工具、接受高波动、愿意做功课的主动型玩家 |

---

## 📖 开场故事：同一份 CPI，有人赌方向，有人只赌“比预期高还是低”

Sara 以前在 CPI 公布日的典型操作：

- 看新闻标题：通胀很高，应该利空风险资产；  
- 或看到社交媒体喊“本次 CPI 会爆冷”，就直接全仓做空/做多；  
- 结果经常是：  
  - 数据刚公布，价格先反方向冲一大波，把她止损扫掉；  
  - 然后再往她原来的方向走。  

后来她换了一套玩法：

1. 会前几天不预判具体数字，而是收集“市场共识”：  
   - 华尔街主流预期值（例如同比 3.2%、环比 0.2%）；  
   - 期货市场对于该数据对利率路径影响的隐含价格；  
2. 会前一晚，明确三件事：  
   - 若数据明显高于预期，应更偏鹰派反应；  
   - 若明显低于预期，应更偏鸽派反应；  
   - 若接近预期，则看“细项与措辞”。  
3. 数据公布那一刻，她重点看的是：  
   - 实际值与预期值的“偏差方向与幅度”；  
   - 市场第一反应是否“过度”或“误读”。

她不再想成为预测帝，而是：

> 只在“现实 vs 预期差距明显”时，做一次高波动的小仓位博弈。

长期下来，她的 CPI 交易从“爆仓日”变成了帐户中 **少量但高质量的波动来源**。

---

## 🧠 CPI 对市场的传导逻辑（简化版）

### 1. CPI 是什么？

- CPI（居民消费价格指数）：  
  - 衡量一篮子商品和服务的价格变动；  
  - 通常会公布：  
    - 总体 CPI 同比/环比  
    - 核心 CPI 同比/环比（剔除食品和能源等波动较大的项目）。  

### 2. 为什么 CPI 影响市场？

因为通胀数据是美联储制定利率路径的重要依据之一：

- **通胀超预期偏高**：  
  - 市场会认为美联储需要保持更紧的货币政策；  
  - 可能意味着加息概率上升或降息节奏放缓；  
  - 通常对风险资产（股指、部分加密资产）不利，对美元和收益率有支撑。  

- **通胀低于预期**：  
  - 市场会认为美联储可以更早或更大力度放松；  
  - 对风险资产利好，对黄金、比特币等“通胀对冲资产”短期反应则要看具体叙事。  

> 但关键在于：  
> 市场 **早就在提前交易预期**，  
> 真正可交易的是“现实与预期的偏差”。

---

## 🧩 子策略拆分：三种 CPI 事件玩法

### 模式 A：纯“波动率博弈”——不猜方向，只赌大波动

> 适合不想研究那么多宏观，只想吃“高波动”这一块肉的人。

- 使用工具：  
  - 指数期权（标普/纳指）、黄金/比特币期权等；  
  - 通过买入短期跨式/宽跨策略，做波动率多头。  

- 基本思路：

  1. 公布前几天，观察相关资产的隐含波动率（IV）：  
     - 如果本次 CPI 被市场认为特别关键，IV 往往已经上升；  
  2. 若 IV 仍在合理或偏低区间：  
     - 考虑买入短期跨式（同一到期的看涨 + 看跌）；  
     - 或略微 OTM 的宽跨来降低成本；  
  3. 数据公布后如出现大幅单边或剧烈震荡：  
     - 在波动放大期分批卖出期权锁定收益；  
  4. 若数据符合预期、波动不及权利金所隐含的幅度：  
     - 策略可能亏损，这就是“赌波动”的自然成本。

---

### 模式 B：偏向方向，但以“预期差”为核心

> 适合对宏观稍有研究，愿意结合预期数据做方向性交易的人。

- 操作步骤（简化）：

  1. 会前 3-5 天：  
     - 收集主流机构、媒体给出的 CPI 预期值范围；  
     - 了解当前市场主线叙事（通胀顽固、趋势向下、二次上行等）。  

  2. 会前 1 天：  
     - 写下“决策表”，例如：  
       - 若总体 CPI 同比高于预期 0.3 个百分点以上，偏鹰；  
       - 若低于预期 0.3 个百分点以上，偏鸽；  
       - 若接近预期，重点看核心 CPI 与细项结构。  

  3. 数据刚公布：  
     - 用最快速度对比“实际值 vs 共识预期”；  
     - 初步判断是“超预期偏鹰”还是“超预期偏鸽”。  

  4. 根据判断在相关资产上做出短周期方向性仓位：  
     - 例如：超预期偏鹰 → 做空股指、做多美元；  
     - 超预期偏鸽 → 做多股指、做多黄金/比特币。  

  5. 持仓时间控制：  
     - 多为数分钟到数小时，不轻易留隔夜；  
     - 避免后续其他数据或官员讲话出现新的打脸信息。

---

### 模式 C：前后两阶段拆分——会前不重仓，会后顺势跟随

> 把“会前高不确定性”和“会后趋势确认”拆成两个不同逻辑的交易。

- 会前阶段：  
  - 轻仓参与，更多是为会后熟悉节奏；  
  - 控制仓位在总计划的 20-30%。  

- 会后阶段：  
  1. 等待数据公布后的第一轮剧烈波动结束（例如 1-5 分钟）；  
  2. 观察哪个方向获胜：  
     - 即价格在剧烈来回后站稳的方向；  
  3. 以小杠杆顺势加入，持有时间从几十分钟到数小时不等；  
  4. 严格设定止损和止盈区间。

---

## 🛠️ CPI 事件日历与“预期差提示器”示意（伪代码）

\`\`\`javascript
class CpiEventHelper {
  constructor(config = {}) {
    this.events = config.events || [];
  }

  /**
   * 事件结构示例：
   * {
   *   date: '2025-02-12',
   *   consensusHeadlineYoY: 3.2,
   *   consensusCoreYoY: 3.5,
   *   consensusHeadlineMoM: 0.2,
   *   consensusCoreMoM: 0.3
   * }
   */
  addEvent(ev) {
    this.events.push(ev);
  }

  listUpcoming(days = 40) {
    const today = new Date();
    return this.events.filter((ev) => {
      const d = new Date(ev.date);
      const diff =
        (d.getTime() - today.getTime()) / (24 * 3600 * 1000);
      return diff >= 0 && diff <= days;
    });
  }

  /**
   * 给定实际数据，计算相对预期的偏差程度
   */
  calcSurprise(ev, actual) {
    const res = {};

    res.headlineYoYDiff =
      actual.headlineYoY - ev.consensusHeadlineYoY;
    res.coreYoYDiff =
      actual.coreYoY - ev.consensusCoreYoY;
    res.headlineMoMDiff =
      actual.headlineMoM - ev.consensusHeadlineMoM;
    res.coreMoMDiff =
      actual.coreMoM - ev.consensusCoreMoM;

    return res;
  }

  /**
   * 根据偏差给出一个简单的“鹰派/鸽派”评分（仅为示意）
   */
  interpretSurprise(diff) {
    let hawkishScore = 0;
    hawkishScore += diff.headlineYoYDiff * 2;
    hawkishScore += diff.coreYoYDiff * 3;
    hawkishScore += diff.headlineMoMDiff * 1;
    hawkishScore += diff.coreMoMDiff * 2;

    let view = 'neutral';
    if (hawkishScore > 0.3) {
      view = 'hawkish';
    } else if (hawkishScore < -0.3) {
      view = 'dovish';
    }

    return { hawkishScore, view };
  }
}
\`\`\`

---

## 📊 风控与仓位控制建议

\`\`\`javascript
const CPI_EVENT_RISK = {
  MAX_EVENT_RISK_RATIO: 0.05,   // 单次 CPI 事件最大风险敞口不超过净资产 5%
  MAX_LEVERAGE: 2,              // 整体验证不建议超过 2 倍杠杆
  MAX_INTRADAY_LOSS: 0.05,      // 当日亏损 5% 必须停手
  CLOSE_WITHIN_HOURS: 4         // 通常在数据公布后 4 小时内结束绝大部分仓位
};
\`\`\`

---

## 🎯 实战 Checklist

- [ ] 建立个人 CPI 发布日历，并同步到日程中  
- [ ] 会前阅读至少 1-2 篇机构简报，了解共识预期与分歧点  
- [ ] 写下明确的“数据-反应”决策表，而不是临场凭感觉  
- [ ] 选择适合自己的子策略：买波动、轻方向、会后顺势  
- [ ] 严格执行最大事件仓位与当日亏损上限  

---

## ✅ 小结

**CPI 不是给你预言未来的考试，而是提供一个“预定高波动”的时间点。**

只要你：

- 不在会前 All-in 赌单一结果；  
- 把注意力从“数字绝对大小”转向“现实 vs 预期差”；  
- 尊重自己的仓位和风控规则；  

CPI 公布日就能从“爆仓黑洞”  
变成你事件驱动策略库中的一条 **可控的宏观节点玩法**。`
};

/**
 * 35.5 项目代币空投前布局
 * 在项目宣布空投快照前买入治理代币，获取空投后卖出本币。
 */
const STRATEGY_35_5 = {
  title: '项目代币空投前布局 - 把“空投预期”变成一套系统玩法',
  slug: 'airdrop-pre-snapshot-positioning',
  summary:
    '围绕项目宣布的空投计划（尤其是明确快照时间或规则的活动），在空投前有节奏地买入治理代币，并在获得空投后，通过本币和空投币的组合管理实现套利或波段收益。关键不在于盲目追空投，而是系统化研究：1）空投规则和快照时间；2）估算空投价值与稀释效应；3）何时退出本币和空投资产。做好这些，才能让“空投布局”从盲目 FOMO 变成可复用策略。',
  category: 'event-driven',
  category_l1: 'derivatives',
  category_l2: 'event-driven',
  risk_level: 4,
  apy_min: 15,
  apy_max: 180,
  min_investment: 1000,
  time_commitment: '每周 4-8 小时（跟踪空投消息、评估价值与执行）',
  status: 'published',
  content: `# 项目代币空投前布局 - 把“空投预期”变成一套系统玩法

## 💰 策略概览

| 参数 | 数值 |
|------|------|
| **适用标的** | 已经上线治理代币、宣布有空投计划或有明确积分/点数机制的项目 |
| **起始资金** | 约 1,000 - 30,000 美元（现货为主，必要时可配合对冲） |
| **时间投入** | 每周 4-8 小时：挖信息、算结构、执行与复盘 |
| **预期收益** | 单次活动可能带来 20-100% 甚至更高回报，也可能小亏 |
| **风险等级** | ⚠️⚠️⚠️⚠️ 较高 (4/5) |
| **难度等级** | 中级（项目研究 + 节奏控制） |

---

## 📖 开场故事：同样是追空投，有人赚逻辑，有人亏在节奏

Neo 和 Lian 都喜欢追空投：

- Neo 的玩法：  
  - 看到“XX 项目空投快照即将到来”就直接冲进去买币；  
  - 不看规则、不算价值，只凭别人说“会涨”。  
  - 经常遇到：  
    - 空投当天币价冲高，但他来不及卖；  
    - 或者空投之后项目方抛压/卖盘涌出，币价砸穿买入价。  

- Lian 的玩法：  
  - 会先看三件事：  
    1. 空投规模与规则：快照范围、参与门槛、分配算法；  
    2. 估算空投价值：预期估值区间、流通节奏；  
    3. 对治理代币价格的潜在影响：稀释还是拉高需求。  
  - 如果算下来“空投价值/持仓成本”合理，且规则够清晰，  
    - 才会分批建仓，并预设“空投前后”的退出策略。  

长期下来，Neo 的空投体验是：  
> 有时赚，有时亏，结果看心情。

Lian 的空投体验则更像一套产品线：  
> 她不追所有活动，只挑自己算得清的那些，  
> 每年累积下来，这条线成为她资产组合的重要一块。

---

## 🧠 空投前布局的核心要素

### 1. 空投规则与快照

- **是否公开快照时间**：  
  - 明确的快照时间：容易形成“快照前拉升、快照后抛压”；  
  - 模糊快照或多次快照：对价格影响更平滑。  

- **资格条件**：  
  - 仅持有治理代币？还是要参与质押、LP、投票等？  
  - 是否有“反女巫”和“刷分惩罚”机制？  

- **分配算法**：  
  - 是否按持仓量线性分配？  
  - 是否对头部大户做削减，对小户做加权？  
  - 是否有积分/等级体系？

### 2. 空投价值与稀释

- 可以简单做一个“预估模型”：

  1. 假设空投代币总价值区间（例如按项目 FDV 的某个折扣估算）；  
  2. 再按你可能拿到的份额，估算“单个地址的空投价值”；  
  3. 同时评估空投对项目治理代币带来的：  
     - 潜在新增需求（为了参与空投而买入）；  
     - 未来抛压（空投代币解锁、二级市场销售）。  

> 如果“空投价值”大致能覆盖或超过你为了参与而增加的本币成本，  
> 且项目本身不拉胯，那么策略才值得考虑。

---

## 🧩 子策略拆分：三种空投前布局打法

### 模式 A：快照型空投 - 纯持仓型布局

> 适用于：只要持有治理代币到快照日就有资格的空投。

- 操作思路：

  1. 活动公告后，先观察几天价格与情绪：  
     - 若短期内已被炒作数倍，可考虑等待回调或直接放弃；  
     - 若涨幅可控，流动性良好，可以开始小仓位布局。  

  2. 计算目标规模：  
     - 根据空投规则估算“每一单位持仓能获得的空投价值”；  
     - 控制总投入不超过净资产 5-10%。  

  3. 建仓方式：  
     - 分批买入，避免一次性吃完深度；  
     - 尽量在离快照日还有足够时间的阶段完成布局。  

  4. 快照前后退出节奏：  
     - 方案一：快照前部分减仓，留部分拿空投；  
     - 方案二：快照后快速卖出本币，持有空投币；  
     - 方案三：根据趋势把空投币和本币看作一篮子资产，统一管理。  

---

### 模式 B：积分/任务型空投 - 综合玩法布局

> 适用于：需要参与交互/任务而不是单纯持仓的空投。

- 特点：  
  - 需要完成一系列链上行动（交易、质押、LP、跨链等）；  
  - 治理代币持仓只是其中一部分权重。  

- 操作建议：

  1. 拆解任务清单：  
     - 统计每个任务的成本（Gas、费用、滑点、时间）；  
     - 估算每个任务对空投得分的贡献。  

  2. 制定“性价比矩阵”：  
     - 优先完成“成本低、得分高”的任务；  
     - 对“成本高、收益不确定”的任务要克制。  

  3. 治理代币持仓策略：  
     - 根据橙卡/银卡/金卡等等级门槛设定持仓区间；  
     - 明确目标：是冲最低档空投，还是冲高档位？  

  4. 活动尾声处理：  
     - 若活动接近尾声，项目已经高度拥挤：  
       - 对治理代币持仓要考虑提前减仓，以防结束后“挖矿完暴跌”。  

---

### 模式 C：空投后套利 - 本币与空投币的组合管理

> 空投到手只是开始，真正可交易的是“本币 + 空投币”的整体结构。

- 典型思路：

  - 方案一：  
    - 快速卖出空投币，回收现金，继续持有本币；  

  - 方案二：  
    - 部分卖出空投币覆盖成本，剩余继续博长期；  

  - 方案三：  
    - 同时减持本币与空投币，把空投视为一次“退出机会”。  

- 决策要点：

  1. 项目长期价值与路线图是否仍然清晰、有执行力；  
  2. 空投分发节奏：一次性 vs 多次线性释放；  
  3. 市场对空投的“兑现节奏”：  
     - 是提前卖预期，空投后反而企稳；  
     - 还是空投后才开始真正抛压。

---

## 🛠️ 空投机会管理脚本框架（伪代码）

\`\`\`javascript
class AirdropPlanner {
  constructor(config = {}) {
    this.minScore = config.minScore || 7;
    this.candidates = [];
  }

  /**
   * 项目结构示例：
   * {
   *   name: 'Example Protocol',
   *   symbol: 'EXM',
   *   hasAnnouncedAirdrop: true,
   *   snapshotDate: '2025-04-10', // 若未知可为 null
   *   airdropAllocationRatio: 0.1, // 空投池占总量 10%
   *   estAirdropValueUsd: 50000000,
   *   tvlUsd: 300000000,
   *   tokenMcapUsd: 200000000,
   *   taskComplexity: 2, // 1-5
   *   sybilProtectionLevel: 3 // 1-5
   * }
   */
  addProject(p) {
    this.candidates.push(p);
  }

  /**
   * 粗略打分模型：看空投性价比
   */
  calcScore(p) {
    let score = 0;

    // 1. 空投池规模相对 TVL/市值
    const poolVsTvl = p.estAirdropValueUsd / (p.tvlUsd || 1);
    if (poolVsTvl > 0.05) score += 3;
    else if (poolVsTvl > 0.02) score += 2;
    else if (poolVsTvl > 0.01) score += 1;

    const poolVsMcap = p.estAirdropValueUsd / (p.tokenMcapUsd || 1);
    if (poolVsMcap > 0.2) score += 3;
    else if (poolVsMcap > 0.1) score += 2;
    else if (poolVsMcap > 0.05) score += 1;

    // 2. 任务复杂度与女巫防护（越合理越好）
    if (p.taskComplexity <= 2) score += 2;
    else if (p.taskComplexity <= 3) score += 1;

    if (p.sybilProtectionLevel >= 3) score += 2;

    // 3. 是否官方已确认空投而不是纯传闻
    if (p.hasAnnouncedAirdrop) score += 2;

    return score;
  }

  findHighPriority() {
    return this.candidates
      .map((p) => {
        const s = this.calcScore(p);
        return { ...p, score: s };
      })
      .filter((p) => p.score >= this.minScore)
      .sort((a, b) => b.score - a.score);
  }
}
\`\`\`

---

## 📊 风控与仓位控制建议

\`\`\`javascript
const AIRDROP_POSITIONING_RISK = {
  MAX_SINGLE_PROJECT_RATIO: 0.05,   // 单个空投项目最大投入不超过净资产 5%
  MAX_TOTAL_AIRDROP_BOOK: 0.2,      // 全部空投布局合计不超过净资产 20%
  MAX_LEVERAGE: 1,                  // 空投布局原则上不建议加杠杆
  EXIT_WITHIN_DAYS_AFTER_SNAPSHOT: 7 // 快照后 7 天内完成主要减仓决策
};
\`\`\`

---

## 🎯 实战 Checklist

- [ ] 建立自己的“空投雷达表”：列出项目、规则、时间与评分  
- [ ] 对每个项目做简要估值，估算空投性价比而非只看宣传  
- [ ] 分清“传闻空投”和“官方确认空投”，对前者保持谨慎  
- [ ] 提前写好“快照前/后”的减仓和锁定收益策略  
- [ ] 定期复盘哪些空投玩法是高性价比，哪些是浪费时间与 Gas  

---

## ✅ 小结

**空投前布局，本质是“用有限成本参与项目成长的增量分配”。**

只要你：

- 不盲目追逐所有空投；  
- 把空投视为一个可以评估的资产，而不是情绪彩票；  
- 认真研究规则、估值与节奏；  

这条线就可以从“零散薅羊毛”变成一条 **可持续的事件驱动策略**。`
};

/**
 * 35.6 黑客攻击事件套利
 * 在项目遭受黑客攻击后快速评估损失，抄底被错杀的优质项目。
 */
const STRATEGY_35_6 = {
  title: '黑客攻击事件套利 - 在“安全事故”中区分致命伤与皮外伤',
  slug: 'post-hack-event-arbitrage',
  summary:
    '围绕 DeFi / CeFi / 交易所等项目遭遇黑客攻击、漏洞利用等安全事故时的短期恐慌抛售，快速评估实际经济损失、协议结构与团队应对能力，判断这是“系统性致命伤”还是“可修复皮外伤”，在后者场景下构建有纪律的抄底和反弹套利策略。核心是：争分夺秒的信息收集 + 极度保守的风险假设 + 严格分批建仓与退出。',
  category: 'event-driven',
  category_l1: 'derivatives',
  category_l2: 'event-driven',
  risk_level: 5,
  apy_min: 20,
  apy_max: 300,
  min_investment: 2000,
  time_commitment: '按事件制：每次事件可能需持续 1-3 天高强度关注',
  status: 'published',
  content: `# 黑客攻击事件套利 - 在“安全事故”中区分致命伤与皮外伤

## 💰 策略概览

| 参数 | 数值 |
|------|------|
| **适用标的** | DeFi 协议代币、CeFi 平台币、桥接协议、交易所平台币等 |
| **起始资金** | 约 2,000 - 50,000 美元（主要为现货，慎用杠杆） |
| **时间投入** | 每次事件持续 1-3 天高强度关注，平时低频准备 |
| **预期收益** | 优质项目的“错杀反弹”可能带来 30-200% 的事件收益，但也可能腰斩再腰斩 |
| **风险等级** | ⚠️⚠️⚠️⚠️⚠️ 极高 (5/5) |
| **难度等级** | 高级（安全认知 + 链上分析 + 情绪判断） |

---

## 📖 开场故事：同样是被黑，有的归零，有的“砸出黄金坑”

在 DeFi 历史中，黑客事件几乎每年都会出现：

- 某个借贷协议因价格预言机被操纵损失数千万美元；  
- 某个跨链桥被提走一大笔资产；  
- 某个 LP 池因合约漏洞被抽空。  

Alice 早期对这些事件只有一个反应：

> “被黑 = 完蛋，赶紧跑。”

后来她观察到两个不同的模式：

1. **致命事件**  
   - 损失占 TVL 或资产的大比例；  
   - 协议核心逻辑本身存在结构性问题；  
   - 团队反应混乱，沟通不透明，甚至跑路；  
   - 这类项目常常从此一蹶不振，价格长期低迷甚至归零。  

2. **可修复事件**  
   - 损失占比虽大，但有风控缓冲或保险基金；  
   - 协议逻辑本身健壮，仅是某个集成模块被 exploit；  
   - 团队快速暂停协议、沟通透明，提出可行的补偿与修复方案；  
   - 一段时间后，TVL 和用户逐步回来，价格从恐慌低点显著修复。  

她开始尝试在第二类事件中：

> 在“别人恐慌砍仓”时，  
> 用自己对协议与损失结构的理解，  
> 分批接一些筹码，  
> 目标是吃到“从极端恐慌到理性修复”的这一段反弹。

当然，并非每次都完美，  
但在严格控制仓位和事件筛选的基础上，  
黑客事件套利成为她策略库中  
一条 **高风险但高赔率、低频出手的玩法**。

---

## 🧠 核心逻辑：从“事故本身”到“可恢复性评估”

### 1. 三个关键问题

1. **损失有多大，占整体多大比例？**  
   - 损失金额 / 协议 TVL  
   - 损失金额 / 协议流通市值  
   - 损失是否集中在特定池子/资产，还是波及核心资产  

2. **协议逻辑本身是否破产？**  
   - 是核心机制被攻破，还是周边模块的配置问题？  
   - 是否可以通过简单参数调整/升级修复？  

3. **团队与社区的应对质量如何？**  
   - 是否第一时间暂停协议、锁住风险？  
   - 是否透明披露事故细节与损失估算？  
   - 是否提出清晰的补偿/恢复计划？  

> 只有在你的回答更偏向：  
> “损失可控 + 逻辑可修复 + 团队可信”，  
> 才有可能谈“错杀抄底”。

---

## 🧩 子策略拆分：三种黑客事件套利打法

### 模式 A：第一阶段不抄底，只做“是否值得跟踪”的筛选

> 真正的风险来自于“搞不清楚发生了什么就急着上车”。

- 事故发生的第一时间，优先做的是 **信息过滤**：

  1. 找到官方渠道（Twitter、Discord、公告）看初步声明；  
  2. 关注第三方安全机构/审计团队的技术分析；  
  3. 在链上浏览器查看攻击者地址与资金流向。  

- 筛选规则（示意）：

  - 若团队消失、刻意模糊、甩锅用户：  
    - 直接标记为“高风险不参与”；  
  - 若安全团队确认漏洞在协议核心、结构性漏洞难修：  
    - 非专业安全研究员最好直接放弃。  

> 第一阶段你只做“是否值得继续跟踪”，  
> 绝不在信息极度不透明时重仓抄底。

---

### 模式 B：第二阶段分批建仓——在极端恐慌区间慢慢出手

> 一旦你确认这是“可修复的皮外伤”而不是“致命伤”，  
> 才会进入真正的建仓阶段。

- 操作思路：

  1. 观察价格从事故发生到数小时/数天内的走势：  
     - 是否出现一次性快速砸盘后横盘；  
     - 是否伴随极高成交量的“换手”；  
  2. 根据事件严重程度，设定一个“悲观估值区间”：  
     - 例如：相对事故前高点跌幅 60-80% 区间；  
  3. 在这个区间内，采用 **分批建仓** 而不是一次性重仓。  

- 建仓节奏示例：

  - 在跌幅达到 50% 时买入计划仓位的 20%；  
  - 跌幅 60-70% 时再加 20-30%；  
  - 若进一步深跌但信息仍可控，再考虑加到总计划仓位；  
  - 若后续出现新的负面信息或团队表现极差，则停止甚至止损。

---

### 模式 C：第三阶段退出——不要把事件博弈变成长期恋爱

> 黑客事件套利是一次性事件交易，  
> 不是长期价值投资（除非你本来就看好项目）。

- 退出策略可以这样思考：

  1. 设定明确的目标区间：  
     - 例如从最低点反弹 50-100% 区间开始分批卖出；  
     - 或回到事故前价格的某个百分比。  

  2. 当关键里程碑出现时要果断减仓：  
     - 安全升级完成、协议重新上线；  
     - 赔付/补偿计划开始执行；  
     - TVL 明显回流。  

  3. 事后复盘：  
     - 该项目是否值得保留一小部分长期仓位；  
     - 还是彻底退出，把事件收益锁定为现金。

---

## 🛠️ 黑客事件评估与筛选脚本框架（伪代码）

\`\`\`javascript
class HackEventFilter {
  constructor(config = {}) {
    this.minQualityScore = config.minQualityScore || 7;
    this.events = [];
  }

  /**
   * 事件数据结构示例：
   * {
   *   protocol: 'ExampleLend',
   *   symbol: 'EXL',
   *   lossUsd: 50000000,
   *   tvlUsdBefore: 300000000,
   *   mcapUsdBefore: 200000000,
   *   isCoreLogicAffected: false,
   *   teamResponseScore: 4, // 1-5 (反应速度、透明度)
   *   securityPartnerInvolved: true
   * }
   */
  addEvent(e) {
    this.events.push(e);
  }

  calcImpactScore(e) {
    const lossVsTvl = e.lossUsd / (e.tvlUsdBefore || 1);
    const lossVsMcap = e.lossUsd / (e.mcapUsdBefore || 1);

    let severity = 0;
    if (lossVsTvl > 0.5) severity += 3;
    else if (lossVsTvl > 0.2) severity += 2;
    else if (lossVsTvl > 0.1) severity += 1;

    if (lossVsMcap > 0.7) severity += 3;
    else if (lossVsMcap > 0.3) severity += 2;
    else if (lossVsMcap > 0.15) severity += 1;

    return { lossVsTvl, lossVsMcap, severity };
  }

  calcQualityScore(e) {
    const impact = this.calcImpactScore(e);
    let score = 0;

    // 损失相对规模：越小越好
    if (impact.lossVsTvl < 0.2) score += 3;
    else if (impact.lossVsTvl < 0.4) score += 2;

    if (impact.lossVsMcap < 0.3) score += 2;
    else if (impact.lossVsMcap < 0.5) score += 1;

    // 核心逻辑是否受影响
    if (!e.isCoreLogicAffected) score += 3;

    // 团队应对与安全伙伴
    score += (e.teamResponseScore || 0); // 1-5 分
    if (e.securityPartnerInvolved) score += 1;

    return { impact, qualityScore: score };
  }

  findPotentialOpportunities() {
    return this.events
      .map((e) => {
        const { impact, qualityScore } = this.calcQualityScore(e);
        return { ...e, ...impact, qualityScore };
      })
      .filter((e) => e.qualityScore >= this.minQualityScore)
      .sort((a, b) => b.qualityScore - a.qualityScore);
  }
}
\`\`\`

---

## 📊 风控与仓位控制建议

\`\`\`javascript
const HACK_EVENT_RISK = {
  MAX_SINGLE_PROTOCOL_RATIO: 0.03, // 单个事件最大风险敞口不超过净资产 3%
  MAX_TOTAL_HACK_BOOK: 0.1,       // 所有黑客事件套利合计不超过净资产 10%
  MAX_LEVERAGE: 1,                // 原则上禁止在黑客事件套利中使用杠杆
  MIN_HOLDING_DAYS: 1,            // 至少给协议 1 天时间确认信息与方案
  MAX_HOLDING_DAYS: 30            // 通常在 30 天内结束整次事件交易
};
\`\`\`

---

## 🎯 实战 Checklist

- [ ] 建立“安全事件监控列表”：包含常见 DeFi 协议、桥、CEX 平台币等  
- [ ] 订阅安全机构、审计团队、慢雾/PeckShield 等的预警信息渠道  
- [ ] 每次事件先回答三个问题：损失规模、逻辑是否致命、团队是否可靠  
- [ ] 只在“可修复 + 团队靠谱”的项目中考虑错杀抄底，且必须分批出手  
- [ ] 为每次事件设定明确退出区间与最长持仓天数，避免无限纠缠  

---

## ✅ 小结

**黑客事件套利，本质是在“他人恐惧”中做一次冷静的定价纠正。**

但前提是：

- 你真的有能力区分“致命伤 vs 皮外伤”；  
- 你能接受这类事件中仍然存在被误判和踩雷的可能；  
- 你愿意用小仓位、长周期的复盘，慢慢积累经验。  

如果这些前提都满足，  
黑客事件就可以从“避之不及的黑天鹅”  
变成你事件驱动策略笔记中的一条 **极其谨慎但高赔率的专项玩法**。`
};

/**
 * 上传 35.4 / 35.5 / 35.6 策略到 Directus
 */
async function uploadStrategies() {
  const DIRECTUS_URL = 'http://localhost:8055';

  console.log('开始上传策略 35.4 / 35.5 / 35.6...\n');

  try {
    // 获取新的管理员令牌
    const { execSync } = require('child_process');
    const tokenOutput = execSync('./get-new-directus-token.sh').toString();
    const tokenMatch = tokenOutput.match(/DIRECTUS_ADMIN_TOKEN=(.+)/);

    if (!tokenMatch) {
      throw new Error('Failed to get admin token');
    }

    const ADMIN_TOKEN = tokenMatch[1].trim();

    const headers = {
      Authorization: `Bearer ${ADMIN_TOKEN}`,
      'Content-Type': 'application/json'
    };

    // 上传策略 35.4
    console.log('上传策略 35.4: CPI 数据公布博弈...');
    await axios.post(`${DIRECTUS_URL}/items/strategies`, STRATEGY_35_4, {
      headers
    });
    console.log('✅ 策略 35.4 上传成功\\n');

    // 上传策略 35.5
    console.log('上传策略 35.5: 项目代币空投前布局...');
    await axios.post(`${DIRECTUS_URL}/items/strategies`, STRATEGY_35_5, {
      headers
    });
    console.log('✅ 策略 35.5 上传成功\\n');

    // 上传策略 35.6
    console.log('上传策略 35.6: 黑客攻击事件套利...');
    await axios.post(`${DIRECTUS_URL}/items/strategies`, STRATEGY_35_6, {
      headers
    });
    console.log('✅ 策略 35.6 上传成功\\n');

    // 验证总数
    const response = await axios.get(
      `${DIRECTUS_URL}/items/strategies?limit=1&meta=total_count`,
      { headers }
    );
    console.log(
      `✅ 数据库中现有策略总数: ${response.data.meta.total_count}`
    );
  } catch (error) {
    console.error('❌ 上传失败:', error.response?.data || error.message);
    process.exit(1);
  }
}

// 若需要直接在 Node 中运行本文件写入策略，可以解除下面一行注释：
uploadStrategies();
