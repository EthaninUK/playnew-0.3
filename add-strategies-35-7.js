// 策略 35.7 & 35.8 & 35.9: 事件驱动 (event-driven-strategies)

const axios = require('axios');

/**
 * 35.7 监管新闻交易
 * 在重大监管新闻（如 SEC 诉讼/ETF 批准）发布后快速反应交易。
 */
const STRATEGY_35_7 = {
  title:
    '监管新闻交易 - 用“政策锚点”和“情绪冲击波”做事件驱动博弈',
  slug: 'regulation-news-event-trade',
  summary:
    '围绕重大监管新闻（SEC/ETF/税收政策/禁令/豁免等）对加密资产价格的短期冲击与中期再定价过程，构建一套“先读预期，再看落地，再做节奏”的事件驱动策略。核心不是“赌监管态度”，而是：1）识别市场事前的共识预期；2）在新闻落地后判断其是“超预期利空/利好”还是“靴子落地”；3）结合不同币种与板块的敏感度差异，做方向性或相对价值交易。需对政策节奏和市场情绪都有基本把握，风险极高。',
  category: 'event-driven',
  category_l1: 'derivatives',
  category_l2: 'event-driven',
  risk_level: 5,
  apy_min: 15,
  apy_max: 250,
  min_investment: 2000,
  time_commitment:
    '按事件制：每月 0-10 小时不等（取决于监管事件密度）',
  status: 'published',
  content: `# 监管新闻交易 - 用“政策锚点”和“情绪冲击波”做事件驱动博弈

## 💰 策略概览

| 参数 | 数值 |
|------|------|
| **适用标的** | BTC / ETH / 主流山寨币 / 板块指数 / 相关股票或 ETF |
| **起始资金** | 约 $2,000 - $50,000（合约/期权需严格控制杠杆） |
| **时间投入** | 按事件制：某些月份可能几乎没有交易，有时则频繁出手 |
| **预期收益** | 单次事件有机会贡献 5-50% 的帐户波动（含盈亏），极端情况更大 |
| **风险等级** | ⚠️⚠️⚠️⚠️⚠️ 极高 (5/5) |
| **难度等级** | 中高级（需要政策直觉 + 市场经验） |
| **适合人群** | 经常关注宏观与监管新闻、乐于阅读原文、不排斥高波动的主动型玩家 |

---

## 📖 开场故事：同一条 SEC 新闻，有人爆仓，有人只吃中段

一次典型的监管事件：  
- SEC 起诉某头部交易所，或对某类代币定性为“证券”；  
- 或批准/否决现货 ETF、期货 ETF、结构化产品。

事件当天的盘面画风往往是：

- 标题出来的一瞬间：  
  - 整个市场价格急跌或急拉；  
  - 社交媒体刷屏式恐慌或狂欢。  

- 随后 1-3 小时：  
  - 情绪从极端回到部分理性；  
  - 一些资产开始反弹或调整到新的平衡区间。  

- 更长一点时间（几天到几周）：  
  - 市场逐步消化政策影响；  
  - 有的资产从谷底爬升，有的则继续走向衰落。

Tom 和 Navi 对此的玩法截然不同：

- **Tom**：  
  - 新闻一出，看到大家喊“完了，要归零了”，  
    就在恐慌最低点继续砍仓；  
  - 或在利好刚出来时追涨高杠杆多单，被随后的回调扫损。  

- **Navi**：  
  - 事件前：她已经知道这一监管议题在发酵，  
    大致了解市场的“共识预期”；  
  - 事件后：她重点看的是：  
    - 实际落地是否比预期更严厉/更宽松；  
    - 各类资产的“敏感度”和“被错杀程度”；  
  - 她目标不是吃到所有波动，而是 **挑中风险-收益比最好的那一小段**。

做了几年之后，Tom 仍然把监管日视作“爆仓日”，  
而 Navi 则把监管事件纳入了自己的“事件驱动策略库”，  
作为 **少量但高质量的交易机会**。

---

## 🧠 监管新闻交易的核心逻辑

监管事件可交易的不是“监管态度好/坏”本身，  
而是 **“市场预期 vs 实际落地”的差异** + **板块/资产的敏感度差异**。

可以拆成三层：

1. **事前预期层**  
   - 市场普遍认为：  
     - 这次通过 ETF 的概率是 70% 还是 30%？  
     - 这次诉讼是“雷声大雨点小”还是“生死大案”？  

2. **落地结果层**  
   - 实际决议/公告的措辞和细节；  
   - 是否比预期“更鹰派/更鸽派”；  
   - 是否包含出人意料的附加条件/豁免。  

3. **资产敏感度层**  
   - BTC 之于“现货 ETF 批准/否决”；  
   - 平台币之于“交易所诉讼或罚款”；  
   - 某板块代币之于“某类业务的合规框架变化”。

> 监管新闻交易的关键，是在这三层之间搭建一套自己的“预期差模型”，  
> 而不是单纯看媒体标题做情绪交易。

---

## 🔍 可交易的监管事件类型

常见可交易的监管事件包括（举例）：

1. **ETF 相关**  
   - 现货/期货 ETF 批准/否决；  
   - ETF 申请进度更新、延期或补充材料。

2. **执法与诉讼**  
   - 针对交易所、项目方、团队核心成员的诉讼与和解；  
   - “某类代币被认定为证券”的判决或声明。  

3. **许可与牌照**  
   - 某国/地区授予特定交易所/支付公司合规牌照；  
   - 或吊销牌照、限制业务。  

4. **税收与披露规则**  
   - 针对加密资产的资本利得税规则；  
   - 针对交易所/钱包的 KYC/AML 要求。  

5. **直接禁止或重大限制**  
   - 某国宣布禁止/严格限制交易挖矿/稳定币支付等；  
   - 也包括后续的“部分放宽”或豁免。

不同类型事件，对资产价格的影响方式、节奏和持续时间完全不同。

---

## 🧩 子策略拆分：三种监管新闻交易打法

### 模式 A：ETF & 指数类监管事件

> 更适合在 BTC / ETH / 相关指数与股票上做交易。

- 事前准备：  
  1. 跟踪 ETF 审批时间表，列出关键日期（受理、回复、延迟、截止日）；  
  2. 了解市场对“通过概率”的共识（机构报告、期货隐含概率等）；  
  3. 观察衍生品市场（期权 skew、基差）如何定价这次事件。

- 事件落地后，典型可交易模式：

  - **“利好兑现下跌”型**：  
    - ETF 批准是“大家都知道的事”，  
    - 在宣布前价格已涨多次，  
    - 落地当天短期冲高后，获利了结盘涌现。  

  - **“超预期利好/利空”型**：  
    - 附加条件、规模、配套政策明显好/坏于预期；  
    - 市场需要重新定价。  

  - **“靴子落地，消除不确定性”型**：  
    - 无论是通过还是否决，  
    - 关键是“终于有结果了”，  
    - 长期表现可能与短线反应不同。

---

### 模式 B：执法 & 诉讼类监管事件

> 主要影响交易所平台币、特定项目代币以及整体风险偏好。

典型例子：

- SEC 起诉头部 CEX：  
  - 短期内平台币和相关资产大幅波动；  
  - 若后续和解金额合理且业务影响可控，有机会出现“错杀反弹”。  

- 某类代币被认定为“证券”：  
  - 对该类资产的交易路径、合规成本产生影响；  
  - 若判决结果比预期更温和，可能反而利好。

交易要点：

1. 第一时间区分：
   - 是“致命执法”，还是“可和解罚款”；  
   - 是针对个别主体，还是针对整个行业模式。  

2. 看“市场反应是否过头”：
   - 某些代币因“同类标签”被一刀切恐慌抛售；  
   - 这时可以寻找质地不错但被错杀的标的做反弹博弈。

---

### 模式 C：区域性禁令 & 解禁事件

> 某国/地区的强力禁令与后续态度变化，  
> 会对本地交易量、矿业、稳定币流动产生冲击。

常见模式：

- 禁令刚出：  
  - 局部市场交易量萎缩，矿业/OTC 带来抛压；  
- 一段时间后：  
  - 资金与算力迁移到其他友好地区或链上生态，  
  - 某些受益项目和板块会迎来增量需求。  

你可以：

- 在禁令初期观察“谁是直接受伤者，谁是潜在受益者”；  
- 在政策放宽或豁免阶段捕捉“预期反转”的波段机会。

---

## 🛠️ 监管事件日历与预期差评估脚本（伪代码）

\`\`\`javascript
class RegulationEventEngine {
  constructor(config = {}) {
    this.events = config.events || [];
  }

  /**
   * 事件结构示例：
   * {
   *   id: 'sec-btc-etf-approval-2025',
   *   date: '2025-03-15',
   *   type: 'ETF', // 'ETF' | 'Lawsuit' | 'Ban' | 'License' | ...
   *   jurisdiction: 'US',
   *   subject: 'BTC spot ETF',
   *   consensusView: 'likely_approval', // 'likely_approval' | 'likely_reject' | 'uncertain'
   *   marketImpliedProb: 0.7 // 事前“通过”概率（来自期货/期权/投行报告等）
   * }
   */
  addEvent(ev) {
    this.events.push(ev);
  }

  listUpcoming(days = 60) {
    const today = new Date();
    return this.events.filter((ev) => {
      const d = new Date(ev.date);
      const diff =
        (d.getTime() - today.getTime()) / (24 * 3600 * 1000);
      return diff >= 0 && diff <= days;
    });
  }

  /**
   * 在事件落地后，根据结果计算“预期差”
   * result 示例：
   * {
   *   outcome: 'approved', // 'approved' | 'rejected' | 'delayed' | 'settled' ...
   *   details: 'some text for manual reading'
   * }
   */
  calcSurprise(ev, result) {
    let surprise = 0;
    let direction = 'neutral';

    if (ev.type === 'ETF') {
      if (result.outcome === 'approved') {
        surprise = 1 - (ev.marketImpliedProb || 0.5);
        direction = 'positive';
      } else if (result.outcome === 'rejected') {
        surprise = ev.marketImpliedProb || 0.5;
        direction = 'negative';
      } else if (result.outcome === 'delayed') {
        surprise = 0.2;
        direction = 'neutral';
      }
    } else if (ev.type === 'Lawsuit') {
      // 仅示意：根据罚款金额、业务限制强度等等估算
      surprise = 0.5;
      direction = result.outcome === 'settled' ? 'positive' : 'negative';
    }

    return { surprise, direction };
  }

  printUpcomingSummary(days = 60) {
    const list = this.listUpcoming(days);
    console.log('📅 未来监管事件预览：');
    list.forEach((ev) => {
      console.log(
        \`- 日期: \${ev.date} | 类型: \${ev.type} | 标的: \${ev.subject} | 市场共识: \${ev.consensusView} | 通过概率(若适用): \${(
          (ev.marketImpliedProb || 0) * 100
        ).toFixed(1)}%\`
      );
    });
  }
}
\`\`\`

---

## 📊 风控与仓位控制建议

\`\`\`javascript
const REGULATION_NEWS_RISK = {
  MAX_EVENT_RISK_RATIO: 0.05,   // 单次监管事件最大风险敞口不超过净资产 5%
  MAX_TOTAL_BOOK: 0.15,         // 所有监管新闻交易合计不超过净资产 15%
  MAX_LEVERAGE: 2,              // 监管事件附近总体杠杆不超过 2 倍
  MAX_INTRADAY_LOSS: 0.05,      // 单日最大亏损 5% 必须停手
  CLOSE_BEFORE_NEXT_EVENT_DAYS: 3 // 下一次重大监管节点前 3 天尽量降低风险敞口
};
\`\`\`

---

## 🎯 实战 Checklist

- [ ] 建立“监管事件日历”：ETF、诉讼关键节点、牌照/禁令时间点  
- [ ] 对每一类事件，写下自己的“预期差判断模板”，不是临时凭感觉  
- [ ] 只在真正有理解优势的事件中参与，不追随标题党情绪  
- [ ] 严格限制仓位，不把监管日当成“翻身一搏”的赌博日  
- [ ] 会后复盘：预期哪里错了？价格反应与自己想象有何差异？  

---

## ✅ 小结

**监管新闻交易，本质是用“政策锚点”重置预期。**

- 对长期投资者来说，它是改变估值框架的节点；  
- 对事件交易者来说，它是少数可以提前知道时间、但不知道结果的高波动机会。  

只要你：

- 把注意力从“情绪”转到“预期差 + 资产敏感度”；  
- 在仓位和杠杆上做足“最坏情况”预案；  

监管新闻就能从“恐慌源”  
变成你事件驱动策略库中 **高风险但可控的专项玩法**。`
};

/**
 * 35.8 链上大额转账监控
 * 监控 Whale Alert 等工具，发现大额转账到交易所时预判抛售。
 */
const STRATEGY_35_8 = {
  title:
    '链上大额转账监控 - 用“鲸鱼资金流”做情绪与供给的先行指标',
  slug: 'onchain-whale-transfer-monitor',
  summary:
    '通过监控链上大额转账（尤其是从冷钱包/基金/团队地址向交易所地址的大额流入），识别潜在的抛售/解锁/套现行为，提前做出风险防范或短期方向性交易。核心是：1）搭建自己的“地址标签+告警系统”；2）区分“中性转移/内部划转”和“真实卖压前奏”；3）将单一事件与宏观情绪、技术形态结合，不盲目因单笔转账而重仓押注。',
  category: 'event-driven',
  category_l1: 'derivatives',
  category_l2: 'event-driven',
  risk_level: 4,
  apy_min: 10,
  apy_max: 120,
  min_investment: 1000,
  time_commitment:
    '前期 1-2 周搭建监控系统，之后每周 2-6 小时巡检与处理告警',
  status: 'published',
  content: `# 链上大额转账监控 - 用“鲸鱼资金流”做情绪与供给的先行指标

## 💰 策略概览

| 参数 | 数值 |
|------|------|
| **适用标的** | 主流公链上的蓝筹币/热门代币（BTC/ETH/稳定币/平台币/热门项目代币等） |
| **起始资金** | 约 $1,000 - $30,000（可配合小杠杆） |
| **时间投入** | 前期 1-2 周搭建告警与标签体系，之后每周 2-6 小时维护和操作 |
| **预期收益** | 不以单次暴利为目标，而是提升整体风控质量与短线胜率 |
| **风险等级** | ⚠️⚠️⚠️⚠️ 较高 (4/5) |
| **难度等级** | 中级（需要基本链上分析能力与脚本使用能力） |

---

## 📖 开场故事：鲸鱼一转账，散户全慌了

“Whale Alert” 在某天推了这样一条信息：

> 🚨 5,000 BTC transferred from unknown wallet to Binance

K 线立即反应：

- 很多散户看到后，条件反射式地砸盘；  
- 社交媒体到处是“鲸鱼要砸盘”的恐慌言论。  

但现实可能有很多种情况：

1. 这是一笔 OTC 之后的内部划转；  
2. 这是一笔抵押品转入，用作借贷/做市，而非直接卖出；  
3. 这是一笔准备分发/质押/集中的运营地址调整；  
4. 当然，也可能真的是“卖出前奏”。

Dylan 刚入圈时，对这种告警只有一个反应：

> “看到大额转交易所 → 直接跟着砸。”

结果当然：  
- 有几次确实躲过了大跌；  
- 但更多时候是砸在局部低点，然后价格继续上行。

后来他改了做法：

1. 不再盲目 follow“通用鲸鱼告警”，  
   而是自己给关键地址打标签：  
   - 项目方多签地址  
   - 交易所热/冷钱包  
   - 基金/巨鲸已知地址  
   - 智能合约托管地址  

2. 为不同类型的地址定义“行为模式”：  
   - 某基金地址转所 = 高概率卖出；  
   - 某协议金库转到链上另一个合约 = 多为运营操作；  

3. 只在“行为模式与宏观情绪同向放大”时，  
   才考虑增减仓，作为 **风控与短线交易的辅助信号**。

结果，他原本频繁被“虚假鲸鱼抛压”扫来扫去的仓位，  
变成了一套 **更冷静的风险雷达**。

---

## 🧠 核心逻辑：从“单笔大额转账”到“行为模式”识别

### 1. 为什么大额转账值得关注？

因为在链上世界，大额转账往往是：

- 大型机构/早期矿工/项目方/鲸鱼的动作；  
- 他们的进出往往会在短时间内影响浮筹结构。

特别是：

- 从长期冷钱包 → 交易所热钱包  
- 从项目方金库 → 交易所地址  

这类 **方向性转移** 更容易与“卖出意图”挂钩。

---

### 2. 但为什么不能简单等于“要砸盘”？

原因包括：

- 内部划转 / 热冷钱包调整；  
- 合约升级、跨链桥迁移；  
- 项目金库的运营资金管理；  
- OTC 交易的交割；  
- 借贷/做市/LP 操作需要的资金准备。

所以你需要：

> 不将“鲸鱼转账”视为单一信号，  
> 而是放入 **地址标签 + 行为历史 + 市场环境** 的组合中解读。

---

## 🧩 子策略拆分：三种鲸鱼监控玩法

### 模式 A：风控优先 - 把鲸鱼告警当作“减仓提示器”

> 更安全、更适合大部分玩家的用法：  
> 不是为了爆赚，而是为了少亏。

- 做法：

  1. 针对你重仓的资产（比如某平台币/某 DeFi 代币），  
     给项目方地址、团队地址、早期大户地址打好标签；  

  2. 当监控到“项目方多签地址 → 交易所地址”的大额转移时：  
     - 检查过去历史中类似操作后的价格表现；  
     - 若过去多次伴随抛压，可将其视为“提前风控信号”；  

  3. 你可以根据情况：  
     - 先减部分仓位；  
     - 或将杠杆仓位降杠杆；  
     - 或设置更紧的止损。

这种模式不追求精准抄顶，而是：  
**宁愿提前一点减仓，也比完全蒙在鼓里要好。**

---

### 模式 B：情绪放大 + 顺势短线

> 当鲸鱼转账与本就脆弱的情绪叠加时，  
> 有时会产生短期的“踩踏式下行”。

- 条件示例：

  1. 市场整体处于 **高位放量 + 技术结构已偏弱** 的阶段；  
  2. 某头部资产或项目出现“鲸鱼转所 + 负面消息”；  
  3. K 线在短时间内跌破关键支撑，成交量巨增。

- 交易思路：

  - 在确认“情绪开始自我强化”后，  
    用小杠杆顺势做空或减少现货敞口；  
  - 目标是吃下这一段“情绪踩踏”的波段，  
    而不是赌项目归零。

---

### 模式 C：深跌后的“鲸鱼回流”博弈

> 不只是“转出”值得监控，“转入”/“吸筹”同样重要。

- 典型场景：

  1. 资产经历一轮大跌，恐慌情绪充斥；  
  2. 链上出现：  
     - 交易所地址向冷钱包/长期地址的大额转出；  
     - 新的巨鲸地址开始多次买入并集中筹码。  

- 操作思路：

  - 将这些“鲸鱼回流”视为潜在底部信号之一，  
  - 与技术结构（横盘、成交量缩减）配合，  
  - 逐步加仓或停止做空，转而寻找反弹机会。

---

## 🛠️ 简易“鲸鱼监控引擎”脚本框架（伪代码）

\`\`\`javascript
class WhaleMonitor {
  constructor(config = {}) {
    this.exchangeLabels = config.exchangeLabels || []; // 交易所地址列表
    this.watchAddresses = config.watchAddresses || []; // 重点关注鲸鱼/项目方地址
    this.minUsdSize = config.minUsdSize || 1000000; // 监控的大额阈值
  }

  /**
   * 示例转账数据结构：
   * {
   *   txHash: '0x...',
   *   from: '0xfrom',
   *   to: '0xto',
   *   asset: 'USDT',
   *   amount: 1000000,
   *   usdValue: 1000000,
   *   timestamp: 1710000000
   * }
   */
  async fetchRecentTransfers() {
    // TODO: 连接区块浏览器 API 或第三方鲸鱼监控服务
    return [];
  }

  isExchangeAddress(addr) {
    return this.exchangeLabels.includes(addr.toLowerCase());
  }

  isWatchedAddress(addr) {
    return this.watchAddresses.includes(addr.toLowerCase());
  }

  classifyTransfer(tx) {
    const fromEx = this.isExchangeAddress(tx.from);
    const toEx = this.isExchangeAddress(tx.to);
    const fromWatched = this.isWatchedAddress(tx.from);
    const toWatched = this.isWatchedAddress(tx.to);

    let type = 'unknown';

    if (!fromEx && toEx && fromWatched) {
      type = 'whale_to_exchange';
    } else if (fromEx && !toEx && toWatched) {
      type = 'exchange_to_whale';
    } else if (fromEx && toEx) {
      type = 'exchange_internal';
    } else if (!fromEx && !toEx && (fromWatched || toWatched)) {
      type = 'whale_internal';
    }

    return type;
  }

  async scanAndAlert() {
    const transfers = await this.fetchRecentTransfers();
    const important = transfers.filter(
      (tx) => tx.usdValue >= this.minUsdSize
    );

    important.forEach((tx) => {
      const type = this.classifyTransfer(tx);
      if (type === 'whale_to_exchange') {
        console.log(
          \`🚨 鲸鱼可能向交易所转入: \${tx.asset} \${tx.usdValue} USD | from \${tx.from} → \${tx.to}\`
        );
      } else if (type === 'exchange_to_whale') {
        console.log(
          \`✅ 交易所向鲸鱼/长期地址转出: \${tx.asset} \${tx.usdValue} USD | from \${tx.from} → \${tx.to}\`
        );
      } else {
        console.log(
          \`ℹ️ 大额转账 (\${type}): \${tx.asset} \${tx.usdValue} USD | \${tx.from} → \${tx.to}\`
        );
      }
    });

    return important;
  }
}
\`\`\`

---

## 📊 风控与仓位控制建议

\`\`\`javascript
const WHALE_MONITOR_RISK = {
  MAX_POSITION_ADJUST_PER_SIGNAL: 0.02, // 单次鲸鱼信号触发时，调整仓位不超过净资产 2%
  MAX_DAILY_ADJUST_RATIO: 0.05,        // 单日由于鲸鱼信号引发的总仓位调整不超过净资产 5%
  MIN_CONFIRMATION_SIGNALS: 2,         // 至少需要 2 个以上相关信号共振（如价格结构+鲸鱼转账）
  IGNORE_BELOW_USD: 500000,            // 忽略 50 万美金以下的转账，减少噪音
  DISABLE_LEVERAGE_FOR_PURE_SIGNAL: true // 纯粹基于单一鲸鱼告警，不建议开杠杆
};
\`\`\`

---

## 🎯 实战 Checklist

- [ ] 为你重点关注的资产建立“地址画像”：交易所、项目方、基金、鲸鱼等  
- [ ] 搭建基础的鲸鱼告警系统：Webhook/Telegram Bot/邮件等皆可  
- [ ] 为不同类型转账写好“行为解释模板”，避免临场过度解读  
- [ ] 只将鲸鱼信号作为风控与辅助方向指标，不做唯一决策依据  
- [ ] 定期复盘鲸鱼信号的命中率：哪些类型真正有预测价值，哪些只是噪音  

---

## ✅ 小结

**链上大额转账监控，不是“看到就跑”，而是“看懂再动”。**

当你：

- 把地址标签做细；  
- 把行为模式总结清楚；  
- 把仓位调整规则写死，而不是事后找理由；  

鲸鱼告警就能从“情绪放大器”，  
变成你交易体系中一个 **冷静的先行风险雷达与辅助 alpha 源**。`
};

/**
 * 35.9 项目 Roadmap 里程碑
 * 在项目重要 Roadmap 节点（如主网上线/合作公告）前布局，利用预期获利。
 */
const STRATEGY_35_9 = {
  title:
    '项目 Roadmap 里程碑 - 把“时间线上的故事节点”变成可交易计划',
  slug: 'project-roadmap-milestone-trade',
  summary:
    '围绕项目 Roadmap 中的重要里程碑（主网上线、主网升级、重大合作、产品落地、跨链集成、代币经济更新等），在预期逐步升温与验证节点前后，进行计划性的布局和退出。核心是：1）系统化管理多个项目的时间线；2）评估每个里程碑对“基本面、叙事、资金”的实际意义；3）区分“纯叙事拉盘”和“真落地放量”，把有限精力集中在高质量项目与高含金量的里程碑上。',
  category: 'event-driven',
  category_l1: 'derivatives',
  category_l2: 'event-driven',
  risk_level: 4,
  apy_min: 15,
  apy_max: 150,
  min_investment: 1000,
  time_commitment:
    '前期搭建“项目里程碑看板”，之后每周 2-6 小时进行更新与布局',
  status: 'published',
  content: `# 项目 Roadmap 里程碑 - 把“时间线上的故事节点”变成可交易计划

## 💰 策略概览

| 参数 | 数值 |
|------|------|
| **适用标的** | 有清晰 Roadmap 且团队执行力不错的公链/协议/应用项目代币 |
| **起始资金** | 约 $1,000 - $30,000（以现货/轻杠杆为主） |
| **时间投入** | 搭建里程碑看板 1-2 周，之后每周 2-6 小时维护与操作 |
| **预期收益** | 单个项目的单次里程碑可带来 10-50% 的波段机会 |
| **风险等级** | ⚠️⚠️⚠️⚠️ 较高 (4/5) |
| **难度等级** | 中级（需要一定项目研究与信息来源整合能力） |

---

## 📖 开场故事：同一个“主网上线”，有人追在顶上，有人提前下车

项目方时常在 Roadmap 上写：

- Q2：测试网上线  
- Q3：主网上线  
- Q4：某大厂合作/某生态集成  

在散户那里常见的操作是：

1. 距离主网上线还有一个月：  
   - 很多人还没怎么关注；  

2. 主网上线前一两周：  
   - 价格开始明显上升，社群推文、KOL 开始集中宣传；  

3. 主网上线当天：  
   - 一堆人看到“主网上线利好”这四个字，才冲进去买；  
   - 然后经常是“利好兑现，价格冲高回落”。  

Kay 后来总结了一条简单规律：

> 对大多数“可预期的好消息”来说，  
>  市场喜欢 **提前交易预期**，  
>  真正落地当天反而更像一个“兑现/换手”时间点。

于是她把自己的 Roadmap 策略拆成两部分：

1. **预期升温段**：  
   - 在“消息已写在 Roadmap 上，但还没被广泛炒作”时，  
     小仓位提前布局；  

2. **落地兑现段**：  
   - 在落地前后几天，  
     根据价格与情绪走向，  
     分批减仓，而不是在主网上线当日才追高。  

长期下来，她会错过一些极端的“上线当日继续暴拉”的行情，  
但整体上，她用 Roadmap 里程碑构建了一条 **可复用的波段交易线**。

---

## 🧠 核心逻辑：为什么 Roadmap 里程碑可以被交易？

1. **时间可预期**  
   - 与宏观事件类似，Roadmap 里程碑通常有粗略时间点；  
   - 即便项目经常延期，但大致节奏可跟踪。  

2. **信息传播有节奏**  
   - 从“内部开发进展” → “社区预热” → “官方宣发” → “落地”；  
   - 价格往往沿着这个节奏逐步反映。  

3. **对基本面的影响层次不同**  
   - 有些里程碑只是“UI 迭代/小功能补丁”；  
   - 有些则是“跨链/集成/TVL 入口/收益机制升级”；  
   - 需要为不同类型里程碑赋予不同权重。

---

## 🔍 高含金量里程碑 vs 低含金量里程碑

大致可以做一个“含金量评分”：

- **高含金量（重点关注）**  
  - 主网或主产品首次上线；  
  - 链上 TVL/用户/收益会显著变化的功能或集成；  
  - 与头部生态（主流公链、大型交易所、大厂）达成实际合作；  
  - 代币经济学的重大升级（如实质性销毁/分红机制）。  

- **中等含金量**  
  - 测试网/Devnet 对外开放；  
  - 常规功能迭代但能提升用户体验/性能；  
  - 与中腰部项目/平台的集成。  

- **低含金量（更多是 PR）**  
  - 社区活动、线下会议赞助、单纯改 Logo/改品牌；  
  - 没有明确量化指标支撑的“生态合作框架协议”。

你的时间与资金应主要投向 **高+中等含金量里程碑**。

---

## 🧩 子策略拆分：三种 Roadmap 玩法

### 模式 A：多项目“里程碑看板” + 分散预期布局

> 把 Roadmap 玩成一个 **多项目时间线组合**，  
> 而不是在单一项目上 All-in。

- 步骤：

  1. 选出一批你看得懂、团队靠谱、有一定流动性的项目；  
  2. 从官网、白皮书、GitHub、AMA 中整理它们的 Roadmap；  
  3. 用表格或看板工具（Notion/Trello/自建前端）记录：  
     - 里程碑名称、预期时间、含金量评分、可能影响的指标。  

- 操作逻辑：

  - 在“距离高含金量里程碑还有 1-3 个月、价格尚未大幅拉升”时，  
    对部分项目进行 **小仓位预期布局**；  
  - 通过多项目分散，降低“单项目延期/翻车”的风险。  

---

### 模式 B：单项目深度跟踪 + 节奏交易

> 对少数认知优势明显的项目做深度跟踪，  
> 用 Roadmap 节点作为节奏锚点。

- 操作示意：

  1. 对该项目进行长期研究：  
     - 技术路线、团队背景、竞品格局；  
     - 核心指标（TVL、活跃地址、协议收入等）。  

  2. 对每个重要里程碑，写下自己的“预期与交易计划”：  
     - 若主网上线顺利，是否考虑持有到下一里程碑？  
     - 若出现严重延期，是否提前减仓？  

  3. 在里程碑前后进行波段操作：  
     - 预期升温期：逐步加仓；  
     - 落地前后几天：观察情绪与成交量，分批减仓或锁盈。  

---

### 模式 C：“利好兑现”后的二次机会

> 很多项目会在重大里程碑落地后，  
> 经历一次“预期兑现 → 价格调整 → 真正落地效果显现”的过程。

- 典型模式：

  1. 落地当天：  
     - 价格短时间冲高，随后出现获利了结；  

  2. 几周后：  
     - 若数据（TVL、链上活跃、收入）持续向好，  
     - 价格可能再走一轮更健康的上升趋势。  

- 你的策略可以是：

  - 落地当天/前后几天 **不再追高**，  
  - 而是在观察真实数据 2-4 周后，再视情况重新建仓，  
  - 把目光从“炒预期”转向“炒实际业绩”。

---

## 🛠️ 项目里程碑管理与评分脚本（伪代码）

\`\`\`javascript
class RoadmapMilestoneEngine {
  constructor(config = {}) {
    this.projects = config.projects || [];
  }

  /**
   * 项目结构示例：
   * {
   *   id: 'l1-x',
   *   name: 'Layer1-X',
   *   symbol: 'L1X',
   *   milestones: [
   *     {
   *       id: 'l1x-mainnet',
   *       title: '主网上线',
   *       estDate: '2025-06-30',
   *       impactScore: 5, // 1-5 (含金量)
   *       type: 'mainnet',
   *       status: 'planned' // 'planned' | 'delayed' | 'completed'
   *     }
   *   ]
   * }
   */
  addProject(p) {
    this.projects.push(p);
  }

  listUpcomingMilestones(days = 90, minImpact = 3) {
    const today = new Date();
    const result = [];

    for (const p of this.projects) {
      for (const m of p.milestones || []) {
        if (m.status !== 'planned') continue;
        const d = new Date(m.estDate);
        const diff =
          (d.getTime() - today.getTime()) / (24 * 3600 * 1000);
        if (diff >= 0 && diff <= days && m.impactScore >= minImpact) {
          result.push({
            projectId: p.id,
            projectName: p.name,
            symbol: p.symbol,
            milestoneId: m.id,
            title: m.title,
            estDate: m.estDate,
            impactScore: m.impactScore,
            daysToGo: diff
          });
        }
      }
    }

    // 按时间与含金量排序
    result.sort((a, b) => {
      if (a.daysToGo === b.daysToGo) {
        return b.impactScore - a.impactScore;
      }
      return a.daysToGo - b.daysToGo;
    });

    console.log('📅 未来高含金量项目里程碑预览：');
    result.forEach((r) => {
      console.log(
        \`- \${r.estDate} | [\${r.symbol}] \${r.projectName} - \${r.title} | 含金量: \${r.impactScore}/5 | 距今天数: \${r.daysToGo.toFixed(
          1
        )}\`
      );
    });

    return result;
  }
}
\`\`\`

---

## 📊 风控与仓位控制建议

\`\`\`javascript
const ROADMAP_MILESTONE_RISK = {
  MAX_PER_PROJECT_RATIO: 0.05,      // 单个项目的里程碑仓位不超过净资产 5%
  MAX_TOTAL_ROADMAP_BOOK: 0.2,      // 整个 Roadmap 策略仓位不超过净资产 20%
  PRE_EVENT_BUILD_RATIO: 0.6,       // 预期升温阶段最多建到目标仓位的 60%
  EVENT_WEEK_ADJUST_RATIO: 0.4,     // 里程碑当周最多再加 40%，避免末日梭哈
  POST_EVENT_CUT_DAYS: 7,           // 里程碑后 7 天内完成主要减仓/锁盈决策
  MAX_PROJECT_COUNT: 10             // 同时深度跟踪的项目不超过 10 个
};
\`\`\`

---

## 🎯 实战 Checklist

- [ ] 先选人再选事：只在你相信团队的项目里做 Roadmap 博弈  
- [ ] 为每个项目建立时间线：测试网、主网、合作、经济学升级等  
- [ ] 为每个里程碑打“含金量分”，仅对高分事件投入大量精力  
- [ ] 提前 1-3 个月开始观察价格与情绪，避免只在落地当天才关注  
- [ ] 里程碑后不恋战：无论结果如何，都要在计划时间内完成复盘与决策  

---

## ✅ 小结

**项目 Roadmap 里程碑，是“叙事与落地”的交汇点。**

- 若你只是看见“主网上线”四个字才冲进去，  
  大概率只是别人提前布局的流动性出口；  

- 若你愿意提前一个季度开始整理时间线、观察节奏、书写计划，  
  Roadmap 就会从一张“漂亮宣传图”，  
  变成你事件驱动策略库里 **可重复利用的时间轴武器**。`
};

/**
 * 上传 35.7 / 35.8 / 35.9 策略到 Directus
 */
async function uploadStrategies() {
  const DIRECTUS_URL = 'http://localhost:8055';

  console.log('开始上传策略 35.7 / 35.8 / 35.9...\n');

  try {
    // 获取新的管理员令牌
    const { execSync } = require('child_process');
    const tokenOutput =
      execSync('./get-new-directus-token.sh').toString();
    const tokenMatch = tokenOutput.match(/DIRECTUS_ADMIN_TOKEN=(.+)/);

    if (!tokenMatch) {
      throw new Error('Failed to get admin token');
    }

    const ADMIN_TOKEN = tokenMatch[1].trim();

    const headers = {
      Authorization: `Bearer ${ADMIN_TOKEN}`,
      'Content-Type': 'application/json'
    };

    // 上传策略 35.7
    console.log('上传策略 35.7: 监管新闻交易...');
    await axios.post(`${DIRECTUS_URL}/items/strategies`, STRATEGY_35_7, {
      headers
    });
    console.log('✅ 策略 35.7 上传成功\\n');

    // 上传策略 35.8
    console.log('上传策略 35.8: 链上大额转账监控...');
    await axios.post(`${DIRECTUS_URL}/items/strategies`, STRATEGY_35_8, {
      headers
    });
    console.log('✅ 策略 35.8 上传成功\\n');

    // 上传策略 35.9
    console.log('上传策略 35.9: 项目 Roadmap 里程碑...');
    await axios.post(`${DIRECTUS_URL}/items/strategies`, STRATEGY_35_9, {
      headers
    });
    console.log('✅ 策略 35.9 上传成功\\n');

    // 验证总数
    const response = await axios.get(
      `${DIRECTUS_URL}/items/strategies?limit=1&meta=total_count`,
      { headers }
    );
    console.log(
      `✅ 数据库中现有策略总数: ${response.data.meta.total_count}`
    );
  } catch (error) {
    console.error('❌ 上传失败:', error.response?.data || error.message);
    process.exit(1);
  }
}

// 若需要在 Node 中直接运行本文件写入策略，可以解除下面一行注释：
uploadStrategies();
