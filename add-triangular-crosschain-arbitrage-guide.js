const axios = require('axios');

const DIRECTUS_URL = 'http://localhost:8055';

/**
 * 建议：生产环境改为使用环境变量
 * const DIRECTUS_EMAIL = process.env.DIRECTUS_EMAIL
 * const DIRECTUS_PASSWORD = process.env.DIRECTUS_PASSWORD
 */

const GUIDE_CONFIG = {
  // ===== 基本信息 =====
  title: '三角 / 跨链套利（Triangular & Cross-Chain Arbitrage）完全指南',
  slug: 'triangular-crosschain-arbitrage-complete-guide',
  summary:
    '覆盖同所三角价差与跨链价差两大场景：路由构建、可实现净边际计算、滑点与深度约束、桥接/到帐时延控制、自动化与风控阈值。',

  // 在 Directus 中替换为真实分类 ID
  category: '874704fb-2611-4417-947b-b7987be572a4',

  // 站内一级/二级分类
  category_l1: 'arbitrage',
  category_l2: '三角 / 跨链套利',

  // ===== 元数据 =====
  difficulty_level: 4,           // 1-5
  risk_level: 4,                 // 延迟/流动性/桥接/执行
  apy_min: 5,
  apy_max: 50,

  // 资金/时间/技术门槛
  threshold_capital: '3,000-150,000 USD（视路由深度与桥接限额）',
  threshold_capital_min: 3000,
  time_commitment: '每日 20–60 分钟（巡检/再平衡/桥接）',
  time_commitment_minutes: 40,
  threshold_tech_level: 'intermediate',

  // ===== 可读性强的 Markdown 正文 =====
  content: `> **适用人群**：已具备多链/多所账户与基础开发能力，希望以“方向中性”捕捉 **同所三角价差** 或 **跨链价差** 的用户  
> **阅读时间**：≈ 12–16 分钟  
> **关键词**：Triangular / Routing / Orderbook / CLMM / Cross-Chain / Bridge Latency / Slippage / Gas / 可实现净边际

---

## 🧭 TL;DR
- **三角套利**：在同一交易所/同一链上，通过 **A→B→C→A** 闭环路由，利用不同交易对的 **相对定价失衡** 赚取 **净价差**。  
- **跨链套利**：在 **链X 与 链Y** 或 **所X 与 所Y** 间利用同一资产的价格错位，通过 **所内对冲 + 桥接迁移** 或 **双边对冲** 获利。  
- **核心度量**：\\*可实现净边际 = 理论价差 − 交易费 − 滑点 − Gas/桥接费 − 到账时延损耗 − 失败回退成本\\*。  
- **纪律**：**同步或先难后易成交**、**失败回退**、**库存与限额阈值**、**Kill-Switch**，小步快跑后再放大。

---

## 🗂 目录
1. [价差来源与基本模型](#价差来源与基本模型)
2. [三角套利：路由与计算](#三角套利路由与计算)
3. [跨链套利：桥接与对冲](#跨链套利桥接与对冲)
4. [成本与可实现净边际](#成本与可实现净边际)
5. [三步极速上手](#三步极速上手)
6. [进阶：库存/限额与自动化](#进阶库存限额与自动化)
7. [风险矩阵与规避清单](#风险矩阵与规避清单)
8. [指标看板与记录模板](#指标看板与记录模板)
9. [FAQ](#faq)
10. [一页执行清单](#一页执行清单)

---

## 📘 价差来源与基本模型
- **撮合/AMM 差异**：订单簿与 CLMM 曲线定价不同步；做市队列与滑点不同。  
- **路由深度差**：A/B/C 任意一边深度不足导致瞬时偏移。  
- **跨链摩擦**：桥接时延、额度限制、桥费/预言机同步差。  
- **事件与区域**：不同链/所的风险偏好与合规限制造成短暂溢价/折价。

---

## 🔺 三角套利：路由与计算
**闭环示例**：在同一所内用 \`USDT\`、\`BTC\`、\`ETH\`  
1) \`USDT → BTC\`（对1）  
2) \`BTC → ETH\`（对2）  
3) \`ETH → USDT\`（对3）

**理论边际（忽略费用与滑点）**  
\`Edge_theory = (P_{USDT→BTC} × P_{BTC→ETH} × P_{ETH→USDT}) − 1\`

**可实现边际**（含费/滑点/Gas）  
\`Edge_real = Edge_theory − Fees_all − Slippage_all − Gas\`

**实践要点**  
- 一侧 **Taker**（先成交流动性更差的一边），其余尽量 **Post-Only Maker**；  
- 以 **盘口Top-N档深度** 估算最大可执行量；  
- 失败回退：任一腿失败 → 立刻逆向平掉已成交腿。

---

## 🌉 跨链套利：桥接与对冲
**两种路径**  
1) **对冲+迁移**：在链A 买入并在链B 同时做空（或反向），锁定价差后 **桥接迁移** 资金，再回补对冲腿；  
2) **直接迁移**：当桥接极快且深度充足，直接从低价链买入桥到高价链卖出。

**关键约束**  
- **桥接延迟/限额**：到帐慢 → 价差衰减；  
- **二次费用**：桥费 + 两链 Gas + 目的链滑点；  
- **可用性**：目的链/所是否有足够深度与稳定交易对。

---

## 🧮 成本与可实现净边际
**统一公式**  
\`Net Edge = Edge_theory − (Fees_Taker + Fees_Maker) − Slippage − Gas_onchain − BridgeFee − LatencyDecay − FailureCost\`

**估算建议**  
- **Fees**：用实际费档+返佣；  
- **Slippage**：按下单量对 Top-N 档深度线性估计（或CLMM公式）；  
- **LatencyDecay**：价差平均持续时间 × 你的执行延迟；  
- **FailureCost**：回退脚本的预估成交冲击。

---

## 🚀 三步极速上手
### 第 1 步：环境与试跑
- 选择 **1–2 家深度好** 的所与 **1–2 条桥接快的链**；  
- 部署 **资金预置**（所内三资产配比；跨链则两链各备稳定币+标的）；  
- 小仓实测 **三角/跨链** 2–3 天，记录 **净边际/命中率/失败率**。

### 第 2 步：阈值化执行
- 仅当 \`Net Edge > 全成本 × (1+安全系数)\` 触发；  
- **先难后易**：优先成交流动性更差/更易消失的腿；  
- 失败回退：任一腿未成 → 立即撤单/对冲，输出回退日志。

### 第 3 步：扩容与分散
- 稳定后增加 **资金/路由/交易对**；  
- 跨链侧增加 **备用桥** 与 **低费链**，错峰迁移；  
- 建立 **一键 Kill-Switch** 与 **库存阈值再平衡**。

---

## ♻️ 进阶：库存/限额与自动化
- **库存区间**：为每个资产与链/所设 \`目标权重 ± 阈值\`，超阈值触发 **所内/跨链再平衡**；  
- **速率限制**：遵守 API Rate Limit，启用 **WebSocket 订阅 + 本地撮合**；  
- **监控告警**：\`NetEdge\`、**拒单率**、**滑点>阈值**、**桥接在途>时限** → 告警/停机；  
- **风险隔离**：关键私钥与 API 只读/交易分权；桥接仅走 **官方或头部桥**。

---

## 🛡️ 风险矩阵与规避清单
**风险矩阵**
| 风险 | 可能性 | 影响 | 说明 |
|---|---|---|---|
| 执行不同步/一腿成交 | 中 | 高 | 价格反向造成方向暴露 |
| 深度不足/滑点过大 | 中 | 中 | 可实现边际被侵蚀 |
| 桥接时延/失败 | 低-中 | 高 | 价差衰减或资金卡在途 |
| 费率与限额变化 | 中 | 中 | 返佣/费档/提现限额调整 |
| 合规/平台事件 | 低-中 | 高 | 宕机/风控/地区限制 |

**规避清单**
- **资金预置 + 所内再平衡** 优先；  
- **先难后易成交，失败必回退**；  
- 仅在 **NetEdge>全成本×(1+安全系数)** 执行；  
- 分散 **桥/链/所**，准备 **备用桥** 与 **低费快链**；  
- 一键 **Kill-Switch**、速率与风控阈值常开。

---

## 📊 指标看板与记录模板
- **可实现净边际**（三角/跨链分开统计）  
- **命中率/失败率/拒单率**  
- **单位资金收益** 与 **周转天数**  
- **库存偏移**（所×链×资产）与再平衡触发日志  
- **桥接在途金额/平均到账时长**

---

## ❓FAQ
**Q1：三角套利一定要高频吗？**  
> 不一定。关键在 **净边际>全成本** 与 **稳定命中率**，再决定频率。  
**Q2：跨链一定要先桥再卖吗？**  
> 不一定。多数情境用 **对冲+迁移** 更稳，先锁定价差再慢慢桥。  
**Q3：做 AMM 上的三角路由可行吗？**  
> 可行，但需考虑 **CLMM 曲线滑点** 与 **多跳 Gas**，尽量 **聚合器估算可实现边际**。

---

## ✅ 一页执行清单
- [ ] 配置所/链账户与资金预置（含备用桥）  
- [ ] 实测 2–3 天：记录净边际/命中率/失败率/在途  
- [ ] 设阈值：NetEdge > 全成本×(1+安全系数) 才执行  
- [ ] 先难后易 + 失败回退 + Kill-Switch  
- [ ] 看板与周报：净边际、库存偏移、桥接在途、单位资金收益
`,

  // ===== 与前端适配的步骤结构（5步） =====
  steps: [
    { step_number: 1, title: '环境与试跑', description: '选择 1–2 家深度好交易所与 1–2 条快桥链，小仓三角/跨链实测。', estimated_time: '2–3 天（实盘）' },
    { step_number: 2, title: '阈值化执行', description: '仅当 NetEdge>全成本×(1+安全系数) 触发；先难后易成交，失败必回退。', estimated_time: '持续' },
    { step_number: 3, title: '库存与再平衡', description: '为所×链×资产设目标权重与阈值；超阈值做所内或跨链再平衡。', estimated_time: '每日 10–20 分钟' },
    { step_number: 4, title: '桥接与在途监控', description: '统计在途金额与平均到账时长；准备备用桥与低费通道。', estimated_time: '10–20 分钟/次' },
    { step_number: 5, title: '扩容与自动化', description: '稳定后加仓加路由；接 WebSocket + 本地撮合；完善告警与 Kill-Switch。', estimated_time: '每周 30–60 分钟' },
  ],
};

// ===== 自动执行：与既有模板一致 =====
async function getAuthToken() {
  const response = await axios.post(`${DIRECTUS_URL}/auth/login`, {
    email: 'the_uk1@outlook.com',
    password: 'Mygcdjmyxzg2026!',
  });
  return response.data.data.access_token;
}

async function addGuide() {
  try {
    const token = await getAuthToken();

    const strategy = {
      ...GUIDE_CONFIG,
      status: 'published',
      is_featured: true,
      view_count: 0,
      bookmark_count: 0,
      published_at: new Date().toISOString(),
    };

    const response = await axios.post(
      `${DIRECTUS_URL}/items/strategies`,
      strategy,
      {
        headers: {
          Authorization: `Bearer ${token}`,
          'Content-Type': 'application/json',
        },
      },
    );

    console.log('\n✅ 三角 / 跨链套利 指南创建成功!');
    console.log(`   ID: ${response.data.data.id}`);
    console.log(`   Slug: ${response.data.data.slug}`);
    console.log(`   访问: http://localhost:3000/strategies/${response.data.data.slug}\n`);
  } catch (error) {
    console.error('\n❌ 创建失败:', error.response?.data || error.message);
  }
}

addGuide();
