# 🎛️ Directus 后台配置说明

**文档版本**: v1.0.0
**创建时间**: 2025-11-17
**适用系统**: PlayPass 积分系统

---

## 📋 概述

PlayPass 系统提供了两个强大的 Directus 后台配置功能，让管理员可以灵活调整系统参数，无需修改代码：

### ✅ 功能 1: 内容定价配置
**配置表**: `playpass_pricing_config`
**功能**: 设置和修改各类内容的 PlayPass 价格
**应用场景**: 策略、套利信号、新闻、八卦等内容的定价

### ✅ 功能 2: PP 奖励规则配置
**配置表**: `playpass_reward_config`
**功能**: 设置和修改获取 PP 的数量和规则
**应用场景**: 签到、阅读、评论、分享等行为的奖励

---

## 🎯 功能 1: 内容定价配置

### 核心字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| **config_key** | 文本 | 规则唯一标识 | `strategy_high_risk` |
| **config_name** | 文本 | 规则中文名称 | `高风险策略定价` |
| **content_type** | 文本 | 内容类型 | `strategy`, `arbitrage`, `news` |
| **pp_price** | 整数 | 基础价格 (Free会员) | `100` |
| **membership_discounts** | JSON | 会员折扣 | `{"0": 1.0, "1": 0.9, "2": 0.7, "3": 0.5, "4": 0.0}` |
| **apply_conditions** | JSON | 适用条件 | `{"risk_level": [4, 5]}` |
| **priority** | 整数 | 优先级 (越大越优先) | `10` |
| **is_active** | 布尔 | 是否启用 | `true` |

### 实际应用示例

#### 示例 1: 高风险策略定价

```json
{
  "config_key": "strategy_high_risk",
  "config_name": "高风险策略定价",
  "content_type": "strategy",
  "pp_price": 100,
  "apply_conditions": {
    "risk_level": [4, 5]
  },
  "priority": 10,
  "is_active": true
}
```

**效果**:
- Free 用户: 100 PP
- Pro 用户 (0.9 折): 90 PP
- Premium 用户 (0.7 折): 70 PP
- Partner 用户 (0.5 折): 50 PP
- MAX 用户: **免费** ✨

---

#### 示例 2: 空投策略免费

```json
{
  "config_key": "strategy_airdrop_free",
  "config_name": "空投策略免费",
  "content_type": "strategy",
  "pp_price": 0,
  "apply_conditions": {
    "category_l1": "airdrop"
  },
  "priority": 5,
  "is_active": true
}
```

**效果**: 所有 category_l1 = "airdrop" 的策略对所有用户免费

---

#### 示例 3: 套利信号分级定价

```json
// 低风险: 30 PP
{
  "config_key": "arbitrage_low_risk",
  "content_type": "arbitrage",
  "pp_price": 30,
  "apply_conditions": {"risk_level": [1, 2]},
  "priority": 5
}

// 中风险: 50 PP
{
  "config_key": "arbitrage_medium_risk",
  "content_type": "arbitrage",
  "pp_price": 50,
  "apply_conditions": {"risk_level": [3]},
  "priority": 6
}

// 高风险: 100 PP
{
  "config_key": "arbitrage_high_risk",
  "content_type": "arbitrage",
  "pp_price": 100,
  "apply_conditions": {"risk_level": [4, 5]},
  "priority": 10
}
```

**效果**: 根据风险等级自动匹配不同价格

---

### 后台操作步骤

#### 创建新定价规则

1. 登录 Directus 后台
2. 进入 **PlayPass Pricing Config** 集合
3. 点击 **➕ Create Item**
4. 填写字段:
   - `config_key`: 英文标识 (唯一)
   - `config_name`: 中文名称
   - `content_type`: 选择内容类型
   - `pp_price`: 输入基础价格
   - `apply_conditions`: 点击 JSON 编辑器,输入条件
   - `priority`: 设置优先级 (可选)
   - `is_active`: 勾选 ✅
5. 点击 **💾 Save**
6. ✅ 立即生效，前端自动应用新定价

#### 修改现有定价

1. 进入 **PlayPass Pricing Config**
2. 找到要修改的规则
3. 点击进入编辑
4. 修改 `pp_price` 字段
5. 点击 **💾 Save**
6. ✅ 实时生效，无需重启

---

## 🎁 功能 2: PP 奖励规则配置

### 核心字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| **reward_key** | 文本 | 奖励唯一标识 | `daily_signin` |
| **reward_name** | 文本 | 奖励中文名称 | `每日签到` |
| **action_type** | 文本 | 行为类型 | `daily_signin`, `read_strategy` |
| **pp_amount** | 整数 | 基础奖励金额 | `10` |
| **apply_multiplier** | 布尔 | 是否应用会员倍率 | `true` |
| **limit_type** | 文本 | 频率限制 | `daily`, `weekly`, `none` |
| **limit_count** | 整数 | 每周期最多次数 | `1` |
| **reward_multiplier** | 小数 | 活动倍数 | `1.0` (普通), `2.0` (双倍) |
| **is_active** | 布尔 | 是否启用 | `true` |
| **icon** | 文本 | 显示图标 | `📅` |

### 实际应用示例

#### 示例 1: 每日签到

```json
{
  "reward_key": "daily_signin",
  "reward_name": "每日签到",
  "action_type": "daily_signin",
  "pp_amount": 10,
  "apply_multiplier": true,
  "limit_type": "daily",
  "limit_count": 1,
  "is_active": true,
  "icon": "📅"
}
```

**实际奖励**:
- Free 用户: 10 × 1.0 = **10 PP**
- Pro 用户: 10 × 1.2 = **12 PP**
- Premium 用户: 10 × 1.5 = **15 PP**
- Partner 用户: 10 × 2.0 = **20 PP**

---

#### 示例 2: 阅读策略

```json
{
  "reward_key": "read_strategy",
  "reward_name": "阅读策略",
  "action_type": "read_strategy",
  "pp_amount": 5,
  "apply_multiplier": true,
  "limit_type": "daily",
  "limit_count": 10,
  "cooldown_seconds": 60,
  "is_active": true,
  "icon": "📖"
}
```

**说明**:
- 每阅读一篇策略奖励 5 PP
- 每天最多 10 次
- 冷却 60 秒 (防止刷新页面刷 PP)
- 应用会员倍率

---

#### 示例 3: 优质评论

```json
{
  "reward_key": "comment_quality",
  "reward_name": "优质评论",
  "action_type": "comment",
  "pp_amount": 20,
  "apply_multiplier": true,
  "limit_type": "daily",
  "limit_count": 10,
  "extra_conditions": {
    "min_length": 100
  },
  "is_active": true,
  "icon": "💬"
}
```

**说明**: 评论长度 ≥ 100 字符才能获得 20 PP

---

#### 示例 4: 发布策略 (高奖励)

```json
{
  "reward_key": "publish_strategy",
  "reward_name": "发布策略",
  "action_type": "publish_strategy",
  "pp_amount": 200,
  "apply_multiplier": true,
  "limit_type": "none",
  "count_towards_daily_limit": false,
  "is_active": true,
  "icon": "✍️"
}
```

**说明**:
- 发布策略通过审核后奖励 200 PP
- 不限次数
- **不计入每日上限** (鼓励内容创作)

---

#### 示例 5: 双倍 PP 活动

```json
{
  "reward_key": "weekend_double_pp",
  "reward_name": "周末双倍 PP",
  "action_type": "read_strategy",
  "pp_amount": 5,
  "apply_multiplier": true,
  "reward_multiplier": 2.0,
  "limit_type": "daily",
  "limit_count": 20,
  "valid_from": "2025-12-21T00:00:00Z",
  "valid_until": "2025-12-22T23:59:59Z",
  "is_active": true,
  "icon": "🎉"
}
```

**说明**:
- 周末双倍 PP 活动
- 阅读策略: 5 × 2.0 × 会员倍率
- 仅在 12/21-12/22 生效

---

### 后台操作步骤

#### 创建新奖励规则

1. 登录 Directus 后台
2. 进入 **PlayPass Reward Config** 集合
3. 点击 **➕ Create Item**
4. 填写字段:
   - `reward_key`: 英文标识 (唯一)
   - `reward_name`: 中文名称
   - `action_type`: 选择行为类型
   - `pp_amount`: 输入基础奖励金额
   - `apply_multiplier`: 勾选 (应用会员倍率)
   - `limit_type`: 选择频率限制
   - `limit_count`: 输入次数限制
   - `is_active`: 勾选 ✅
   - `icon`: 输入 emoji 图标
5. 点击 **💾 Save**
6. ✅ 立即生效

#### 修改奖励金额

1. 进入 **PlayPass Reward Config**
2. 找到要修改的规则
3. 修改 `pp_amount` 字段
4. 点击 **💾 Save**
5. ✅ 实时生效

---

## 🔥 常见操作场景

### 场景 1: 调整策略价格

**需求**: 将高风险策略价格从 100 PP 降至 80 PP

**步骤**:
1. Directus → PlayPass Pricing Config
2. 找到 `strategy_high_risk`
3. `pp_price`: `100` → `80`
4. Save
5. ✅ 前端实时生效

---

### 场景 2: 举办双倍 PP 活动

**需求**: 周末阅读策略双倍奖励

**步骤**:
1. Directus → PlayPass Reward Config
2. 找到 `read_strategy`
3. `reward_multiplier`: `1.0` → `2.0`
4. `valid_from`: `2025-12-21 00:00:00`
5. `valid_until`: `2025-12-22 23:59:59`
6. Save
7. ✅ 活动自动生效和结束

---

### 场景 3: 临时关闭某个奖励

**需求**: 暂时关闭分享内容奖励 (防刷)

**步骤**:
1. Directus → PlayPass Reward Config
2. 找到 `share_content`
3. 取消勾选 `is_active`
4. Save
5. ✅ 用户无法再获得分享奖励

---

### 场景 4: 新增内容类型定价

**需求**: 为 "Gossip" 设置定价

**步骤**:
1. Directus → PlayPass Pricing Config
2. Create Item
3. 填写:
   - `config_key`: `gossip_default`
   - `config_name`: `八卦内容定价`
   - `content_type`: `gossip`
   - `pp_price`: `5`
4. Save
5. ✅ 前端自动应用定价

---

## 📊 统计数据查看

在 Directus 后台可以实时查看统计数据：

### 定价配置统计

| 字段 | 说明 |
|------|------|
| `total_unlocks` | 累计解锁次数 |
| `total_pp_earned` | 累计 PP 收入 |

**示例**:
```
高风险策略定价
- 解锁次数: 1,234 次
- 总收入: 123,400 PP
```

### 奖励规则统计

| 字段 | 说明 |
|------|------|
| `total_completions` | 累计完成次数 |
| `total_pp_distributed` | 累计发放 PP |

**示例**:
```
每日签到
- 完成次数: 12,345 次
- 总发放: 147,678 PP
```

---

## 🎯 最佳实践

### ✅ 定价策略建议

1. **免费内容**: 新手友好的空投、测试网类策略设为免费
2. **低价内容**: 普通 DeFi 策略 50-100 PP
3. **中价内容**: 高级策略、套利信号 100-200 PP
4. **高价内容**: 专家级策略 200+ PP
5. **MAX 免费**: 所有内容对 MAX 会员免费

### ✅ 奖励规则建议

1. **每日签到**: 10 PP (应用倍率)
2. **阅读内容**: 5 PP (限每日 10 次)
3. **分享内容**: 3 PP (限每日 5 次)
4. **优质评论**: 20 PP (长度 ≥ 100 字)
5. **发布策略**: 200 PP (不计入每日上限)
6. **邀请好友**: 50 PP (不计入每日上限)

### ✅ 活动策略建议

1. **双倍 PP 周末**: `reward_multiplier: 2.0`
2. **新人注册礼包**: 一次性 200 PP
3. **节日特别活动**: 临时提高奖励倍数
4. **限时免费**: 临时将某些内容 `pp_price: 0`

---

## 🔧 技术说明

### 定价匹配逻辑

```typescript
// 系统按优先级匹配定价规则
function getPriceForContent(content, user_membership_level) {
  // 1. 查询所有激活的定价规则 (按优先级降序)
  const rules = getPricingRules(content.type);

  // 2. 匹配第一个符合条件的规则
  for (const rule of rules) {
    if (matchesConditions(content, rule.apply_conditions)) {
      // 3. 应用会员折扣
      const discount = rule.membership_discounts[user_membership_level];
      return rule.pp_price * discount;
    }
  }

  // 4. 没有匹配规则返回默认价格
  return 0;
}
```

### 奖励计算逻辑

```typescript
// 奖励金额计算
function calculateReward(action_type, user_membership_level) {
  const rule = getRewardRule(action_type);

  let finalAmount = rule.pp_amount;

  // 1. 应用活动倍数
  finalAmount *= rule.reward_multiplier;

  // 2. 应用会员倍率
  if (rule.apply_multiplier) {
    const multipliers = {0: 1.0, 1: 1.2, 2: 1.5, 3: 2.0, 4: 999.99};
    finalAmount *= multipliers[user_membership_level];
  }

  return Math.round(finalAmount);
}
```

---

## ❓ 常见问题

### Q1: 修改定价后多久生效？
**A**: 立即生效，前端下次查询时自动应用新价格

### Q2: 如何设置限时活动？
**A**: 使用 `valid_from` 和 `valid_until` 字段设置时间范围

### Q3: 如何设置会员专属奖励？
**A**: 设置 `min_membership_level` 字段，例如 `2` 表示 Premium 以上才能获得

### Q4: 如何防止用户刷 PP？
**A**: 设置 `limit_count` (每日限制) 和 `cooldown_seconds` (冷却时间)

### Q5: MAX 会员需要 PP 吗？
**A**: 不需要，MAX 会员所有内容免费，无需消耗 PP

---

## 📝 总结

### ✅ 核心优势

1. **灵活配置**: 无需修改代码，后台实时调整
2. **动态定价**: 支持条件匹配，自动分级定价
3. **活动支持**: 轻松举办双倍 PP、限时免费等活动
4. **数据统计**: 实时查看解锁次数、PP 流水
5. **会员差异化**: 自动应用会员折扣和倍率

### ✅ 适用场景

- ✅ 内容价格调整
- ✅ 会员权益优化
- ✅ 运营活动策划
- ✅ 用户增长激励
- ✅ 数据分析决策

---

**文档版本**: v1.0.0
**最后更新**: 2025-11-17
**作者**: Claude Code (Anthropic)
**完整设计文档**: [PLAYPASS-SYSTEM-DESIGN.md](PLAYPASS-SYSTEM-DESIGN.md)
