# 套利信号板块 - 产品需求文档（PRD）

**版本**: v1.0
**创建日期**: 2025-01-10
**产品经理**: PlayNew Team
**技术负责人**: TBD

---

## 一、产品背景与目标

### 1.1 背景

币圈套利是相对低风险的获利方式，但需要：
- ⚡ **实时性**：价差窗口通常只有几秒到几分钟
- 🎯 **精准性**：需要考虑手续费、滑点、提现时间
- 📊 **多维度**：跨所、跨链、资金费率、期现套利等多种类型
- 🤖 **自动化**：人工监控效率低，需要系统化工具

### 1.2 产品目标

**核心价值**：为用户提供实时、可执行的套利机会信号

**目标用户**：
- 💰 量化交易团队
- 🤝 做市商和流动性提供者
- 📈 专业套利者
- 🎓 学习套利的进阶用户

**关键指标**：
- 信号准确率 > 85%
- 信号延迟 < 3 秒
- 月活跃用户 > 5000
- 会员转化率 > 15%

---

## 二、套利类型与信号定义

### 2.1 支持的套利类型

#### A. 跨所现货套利（Cross-Exchange Spot Arbitrage）
**原理**：同一币种在不同交易所的价格差异

**信号要素**：
```typescript
interface SpotArbitrageSignal {
  type: 'spot_arbitrage';
  tradingPair: string;           // 例如：BTC/USDT
  buyExchange: string;           // 低价所：Binance
  sellExchange: string;          // 高价所：OKX
  buyPrice: number;              // 买入价
  sellPrice: number;             // 卖出价
  spreadPercent: number;         // 价差百分比
  netProfitPercent: number;      // 扣除手续费后净利润
  estimatedProfit: number;       // 预估利润（USDT）
  liquidity: {
    buyVolume: number;           // 可买入量
    sellVolume: number;          // 可卖出量
  };
  fees: {
    buyFee: number;              // 买入手续费
    sellFee: number;             // 卖出手续费
    withdrawalFee: number;       // 提现手续费
  };
  transferTime: number;          // 预估转账时间（分钟）
  riskLevel: 1 | 2 | 3;         // 风险等级
  detectedAt: Date;
  expiresAt: Date;
}
```

**示例信号**：
> 🔄 **BTC/USDT 跨所套利**
> 买入：Binance @ $42,850
> 卖出：OKX @ $43,100
> 价差：0.58% | 净利润：0.31%
> 流动性：5.2 BTC
> ⏱️ 30秒前

#### B. 三角套利（Triangular Arbitrage）
**原理**：同一交易所内，通过三个币种的汇率差异套利

**信号要素**：
```typescript
interface TriangularArbitrageSignal {
  type: 'triangular_arbitrage';
  exchange: string;              // 交易所
  path: string[];                // 例如：['BTC', 'ETH', 'USDT', 'BTC']
  startAmount: number;           // 起始金额
  endAmount: number;             // 最终金额
  profitPercent: number;         // 利润百分比
  tradingPairs: {
    pair: string;
    side: 'buy' | 'sell';
    price: number;
  }[];
  executionTime: number;         // 预估执行时间（秒）
  complexity: 'low' | 'medium' | 'high';
  detectedAt: Date;
}
```

#### C. 资金费率套利（Funding Rate Arbitrage）
**原理**：做多现货 + 做空永续合约，赚取资金费率

**信号要素**：
```typescript
interface FundingRateArbitrageSignal {
  type: 'funding_rate_arbitrage';
  symbol: string;                // BTC-PERP
  exchange: string;
  fundingRate: number;           // 当前资金费率
  nextFundingTime: Date;         // 下次收取时间
  annualizedReturn: number;      // 年化收益率
  spotPrice: number;             // 现货价格
  perpPrice: number;             // 永续合约价格
  basisSpread: number;           // 基差
  recommendedAction: 'long' | 'short' | 'close';
  holdingPeriod: number;         // 建议持有周期（小时）
  riskLevel: 1 | 2 | 3;
  detectedAt: Date;
}
```

#### D. 期现套利（Cash and Carry / Basis Trading）
**原理**：现货与期货价格差异

**信号要素**：
```typescript
interface BasisTradingSignal {
  type: 'basis_trading';
  symbol: string;
  spotPrice: number;
  futuresPrice: number;
  futuresExpiry: Date;
  basisPoints: number;           // 基差点数
  annualizedReturn: number;      // 年化收益
  daysToExpiry: number;
  impliedYield: number;
  riskLevel: 1 | 2 | 3;
  detectedAt: Date;
}
```

#### E. 跨链套利（Cross-Chain Arbitrage）
**原理**：同一资产在不同区块链上的价格差异

**信号要素**：
```typescript
interface CrossChainArbitrageSignal {
  type: 'cross_chain_arbitrage';
  token: string;                 // 例如：USDC
  buyChain: string;              // 例如：Ethereum
  sellChain: string;             // 例如：Arbitrum
  buyPrice: number;
  sellPrice: number;
  spreadPercent: number;
  bridgeFee: number;
  bridgeTime: number;            // 预估跨链时间（分钟）
  gasEstimate: {
    buyGas: number;
    sellGas: number;
    bridgeGas: number;
  };
  netProfit: number;
  detectedAt: Date;
}
```

#### F. 稳定币脱锚套利（Stablecoin Depeg Arbitrage）
**原理**：稳定币偏离锚定价格时的套利机会

**信号要素**：
```typescript
interface DepegArbitrageSignal {
  type: 'depeg_arbitrage';
  stablecoin: string;            // 例如：USDT
  currentPrice: number;          // 当前价格
  pegPrice: number;              // 锚定价格（通常是 1.0）
  depegPercent: number;          // 脱锚百分比
  volume24h: number;
  liquidityPool: string;         // 流动性池
  recoveryProbability: number;   // 回归概率评分
  historicalDepegData: {
    maxDepeg: number;
    avgRecoveryTime: number;     // 平均恢复时间（小时）
  };
  riskLevel: 1 | 2 | 3 | 4 | 5;
  detectedAt: Date;
}
```

---

## 三、页面结构与功能

### 3.1 页面布局

```
┌─────────────────────────────────────────────────────────┐
│  Header: PlayNew.ai Navigation                          │
├─────────────────────────────────────────────────────────┤
│  ┌───────────────┬─────────────────────────────────┐   │
│  │  侧边栏        │  主内容区                        │   │
│  │               │                                  │   │
│  │  [ 套利类型   │  ┌─────────────────────────┐   │   │
│  │    过滤器 ]   │  │  统计面板               │   │   │
│  │               │  │  • 活跃信号数           │   │   │
│  │  [ 交易所     │  │  • 24h 套利机会         │   │   │
│  │    过滤器 ]   │  │  • 平均收益率           │   │   │
│  │               │  └─────────────────────────┘   │   │
│  │  [ 收益率     │                                  │   │
│  │    范围 ]     │  ┌─────────────────────────┐   │   │
│  │               │  │  实时信号列表           │   │   │
│  │  [ 风险等级   │  │  ┌───────────────────┐ │   │   │
│  │    选择 ]     │  │  │ 信号卡片 1        │ │   │   │
│  │               │  │  └───────────────────┘ │   │   │
│  │  [ 仅显示     │  │  ┌───────────────────┐ │   │   │
│  │    VIP信号 ]  │  │  │ 信号卡片 2        │ │   │   │
│  │               │  │  └───────────────────┘ │   │   │
│  └───────────────┘  └─────────────────────────┘   │   │
│                                                      │   │
├─────────────────────────────────────────────────────────┤
│  Footer                                                 │
└─────────────────────────────────────────────────────────┘
```

### 3.2 核心功能模块

#### A. 实时信号流（Real-time Signal Feed）

**功能描述**：
- 实时展示所有检测到的套利信号
- WebSocket 推送，延迟 < 3 秒
- 支持分页和无限滚动
- 自动刷新和声音提醒

**信号卡片设计**：
```tsx
<SignalCard>
  <Header>
    <Badge type="spot_arbitrage" />
    <TimeAgo timestamp={signal.detectedAt} />
    <RiskLevel level={signal.riskLevel} />
  </Header>

  <Body>
    <TradingPair symbol="BTC/USDT" />
    <PriceComparison
      buy={{ exchange: "Binance", price: 42850 }}
      sell={{ exchange: "OKX", price: 43100 }}
    />
    <Metrics>
      <Metric label="价差" value="0.58%" highlight />
      <Metric label="净利润" value="0.31%" positive />
      <Metric label="流动性" value="5.2 BTC" />
    </Metrics>
  </Body>

  <Footer>
    <Button variant="primary">查看详情</Button>
    <Button variant="outline">设置提醒</Button>
    <FavoriteButton />
  </Footer>
</SignalCard>
```

#### B. 高级筛选器（Advanced Filters）

**筛选维度**：
1. **套利类型**
   - [ ] 跨所现货套利
   - [ ] 三角套利
   - [ ] 资金费率套利
   - [ ] 期现套利
   - [ ] 跨链套利
   - [ ] 稳定币脱锚套利

2. **交易所**（多选）
   - [ ] Binance
   - [ ] OKX
   - [ ] Bybit
   - [ ] Gate.io
   - [ ] Huobi
   - ... 更多

3. **收益率范围**
   - 滑块：0.1% - 5%+
   - 快捷选项：> 0.3%, > 0.5%, > 1%

4. **风险等级**
   - [ ] 低风险（1-2星）
   - [ ] 中风险（3星）
   - [ ] 高风险（4-5星）

5. **资金门槛**
   - 滑块：$100 - $100,000+

6. **币种**
   - 搜索框 + 多选
   - 主流币 / 山寨币 / 稳定币

7. **仅VIP信号**
   - Toggle 开关

#### C. 信号详情页（Signal Detail）

**URL**: `/arbitrage/signals/:id`

**内容模块**：

1. **核心信息**
   - 套利类型和币种
   - 价格对比（可视化图表）
   - 收益计算器（输入金额 → 计算利润）

2. **执行步骤**（Step-by-step Guide）
   ```
   步骤 1: 在 Binance 买入 BTC @ $42,850
   步骤 2: 提现 BTC 到 OKX（预计 30 分钟）
   步骤 3: 在 OKX 卖出 BTC @ $43,100
   ```

3. **风险提示**
   - ⚠️ 价格可能在转账过程中变化
   - ⚠️ 提现可能遇到限额或延迟
   - ⚠️ 需要在两个交易所都有账户

4. **历史数据**
   - 该套利对过去 24 小时/7 天的价差走势
   - 出现频率和平均持续时间
   - 成功率统计

5. **相关工具**
   - 交易所链接（快速跳转）
   - 手续费计算器
   - 资金管理建议

#### D. 实时监控面板（Monitoring Dashboard）

**功能**：
- 📊 **套利机会统计**
  - 今日/本周/本月总数
  - 各类型分布（饼图）
  - 平均收益率趋势（折线图）

- 🔥 **热门套利对**
  - Top 10 最频繁出现的套利机会
  - 点击可设置提醒

- 💰 **收益排行**
  - 本周收益率最高的 10 个信号
  - 包含详情链接

- ⏰ **信号时效性分析**
  - 信号的平均存续时间
  - 最佳执行时段分析

#### E. 提醒系统（Alert System）

**提醒方式**：
- 🔔 浏览器推送通知
- 📧 邮件提醒
- 📱 Telegram Bot
- 🔗 Webhook（高级用户）

**提醒条件设置**：
```typescript
interface AlertCondition {
  type: ArbitrageType[];         // 关注的套利类型
  exchanges?: string[];          // 特定交易所
  symbols?: string[];            // 特定币种
  minProfitPercent: number;      // 最低收益率
  minLiquidity?: number;         // 最低流动性
  maxRiskLevel?: number;         // 最高风险等级
  onlyVIP?: boolean;
}
```

**示例提醒**：
> 🚨 **套利信号提醒**
> BTC/USDT 跨所套利机会
> 收益率：0.85% | 风险：⭐⭐
> 立即查看：https://playnew.ai/arbitrage/signals/abc123

#### F. 套利策略教程（Strategy Guides）

**内容**：
- 📚 各类套利的详细教程
- 🎥 视频教学
- 💡 风险管理建议
- 📈 真实案例分析
- 🤖 自动化套利方案

---

## 四、技术架构

### 4.1 前端架构

**技术栈**：
- Next.js 15 (App Router)
- React 18
- TypeScript
- TailwindCSS
- Shadcn/ui
- WebSocket (实时数据)
- React Query (数据管理)
- Recharts (图表可视化)

**目录结构**：
```
frontend/
├── app/
│   ├── arbitrage/
│   │   ├── page.tsx                    # 套利信号列表页
│   │   ├── signals/
│   │   │   └── [id]/
│   │   │       └── page.tsx            # 信号详情页
│   │   ├── dashboard/
│   │   │   └── page.tsx                # 监控面板
│   │   ├── alerts/
│   │   │   └── page.tsx                # 提醒设置
│   │   └── guides/
│   │       └── [slug]/
│   │           └── page.tsx            # 策略教程
│   └── api/
│       └── arbitrage/
│           ├── signals/
│           │   ├── route.ts            # 获取信号列表
│           │   └── [id]/
│           │       └── route.ts        # 获取单个信号
│           ├── alerts/
│           │   └── route.ts            # 提醒管理
│           └── ws/
│               └── route.ts            # WebSocket 连接
├── components/
│   ├── arbitrage/
│   │   ├── SignalCard.tsx              # 信号卡片
│   │   ├── SignalFilters.tsx           # 筛选器
│   │   ├── SignalDetail.tsx            # 详情组件
│   │   ├── ProfitCalculator.tsx        # 收益计算器
│   │   ├── ExecutionSteps.tsx          # 执行步骤
│   │   ├── AlertSetup.tsx              # 提醒设置
│   │   └── StatisticsPanel.tsx         # 统计面板
│   └── charts/
│       ├── SpreadChart.tsx             # 价差图表
│       └── DistributionChart.tsx       # 分布图表
├── lib/
│   ├── arbitrage/
│   │   ├── types.ts                    # TypeScript 类型定义
│   │   ├── calculations.ts             # 收益计算逻辑
│   │   ├── filters.ts                  # 筛选逻辑
│   │   └── websocket.ts                # WebSocket 客户端
│   └── utils/
│       ├── formatters.ts               # 数据格式化
│       └── validators.ts               # 数据验证
└── hooks/
    ├── useArbitrageSignals.ts          # 信号数据 Hook
    ├── useWebSocket.ts                 # WebSocket Hook
    └── useSignalAlerts.ts              # 提醒 Hook
```

### 4.2 后端架构

**技术栈**：
- Node.js / Python（信号检测引擎）
- PostgreSQL（数据存储）
- Redis（缓存 + 实时数据）
- WebSocket Server（实时推送）
- Directus（CMS + API）

**数据库设计**：

```sql
-- 套利信号表
CREATE TABLE arbitrage_signals (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  type VARCHAR(50) NOT NULL,           -- 套利类型
  symbol VARCHAR(20) NOT NULL,         -- 交易对
  data JSONB NOT NULL,                 -- 信号详细数据（不同类型结构不同）
  profit_percent DECIMAL(8,4),         -- 收益率
  risk_level INTEGER,                  -- 风险等级 1-5
  liquidity_usd DECIMAL(18,2),         -- 流动性（美元）
  status VARCHAR(20) DEFAULT 'active', -- active / expired / executed
  detected_at TIMESTAMP DEFAULT NOW(),
  expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_signals_type ON arbitrage_signals(type);
CREATE INDEX idx_signals_symbol ON arbitrage_signals(symbol);
CREATE INDEX idx_signals_profit ON arbitrage_signals(profit_percent DESC);
CREATE INDEX idx_signals_detected ON arbitrage_signals(detected_at DESC);
CREATE INDEX idx_signals_status ON arbitrage_signals(status);

-- 用户提醒配置表
CREATE TABLE user_alerts (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES users(id),
  name VARCHAR(100),                   -- 提醒名称
  conditions JSONB NOT NULL,           -- 提醒条件
  channels JSONB NOT NULL,             -- 提醒渠道（email, telegram, webhook）
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 信号历史记录表
CREATE TABLE signal_history (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  signal_id UUID REFERENCES arbitrage_signals(id),
  snapshot_data JSONB NOT NULL,        -- 信号快照
  captured_at TIMESTAMP DEFAULT NOW()
);

-- 交易所配置表
CREATE TABLE exchanges (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name VARCHAR(50) NOT NULL UNIQUE,
  api_enabled BOOLEAN DEFAULT true,
  fees JSONB,                          -- 手续费配置
  withdrawal_times JSONB,              -- 提现时间
  metadata JSONB
);
```

### 4.3 信号检测引擎

**架构**：
```
┌─────────────────────────────────────────┐
│  Data Collection Layer                  │
│  ┌────────┐ ┌────────┐ ┌────────┐      │
│  │ CEX API│ │ DEX API│ │Chain  │      │
│  │ Client │ │ Client │ │Scanner│      │
│  └────────┘ └────────┘ └────────┘      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Signal Detection Engine                │
│  ┌────────────────────────────────┐    │
│  │ Spot Arbitrage Detector        │    │
│  │ Triangular Arbitrage Detector  │    │
│  │ Funding Rate Analyzer          │    │
│  │ Basis Trading Detector         │    │
│  │ Cross-Chain Detector           │    │
│  │ Depeg Detector                 │    │
│  └────────────────────────────────┘    │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Filtering & Validation                 │
│  • Minimum Profit Threshold             │
│  • Liquidity Check                      │
│  • Risk Assessment                      │
│  • Duplicate Detection                  │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Signal Distribution                    │
│  • Database Storage                     │
│  • WebSocket Broadcast                  │
│  • Alert Trigger                        │
│  • API Exposure                         │
└─────────────────────────────────────────┘
```

**检测频率**：
- 现货套利：每 3-5 秒
- 资金费率：每 5 分钟
- 期现套利：每 10 分钟
- 三角套利：每 5 秒
- 跨链套利：每 30 秒

---

## 五、会员权益设计

### 5.1 免费用户

**权益**：
- ✅ 查看基础套利信号（延迟 5 分钟）
- ✅ 每日限额 10 个信号
- ✅ 查看套利教程
- ❌ 无实时推送
- ❌ 无高级筛选
- ❌ 无提醒功能

### 5.2 付费会员

**权益**：
- ✅ **实时信号**（延迟 < 3 秒）
- ✅ **无限信号查看**
- ✅ **高级筛选器**
- ✅ **实时提醒**（浏览器 + 邮件 + Telegram）
- ✅ **收益计算器**
- ✅ **历史数据分析**
- ✅ **VIP 专属信号**（高收益低风险）
- ✅ **API 访问**（高级会员）
- ✅ **自动化执行方案**（终身会员）

---

## 六、UI/UX 设计规范

### 6.1 配色方案

```css
/* 套利信号专用色彩 */
--arbitrage-primary: #10b981;      /* 主色调：绿色（盈利） */
--arbitrage-secondary: #3b82f6;    /* 次要色：蓝色 */
--arbitrage-danger: #ef4444;       /* 危险：红色（高风险） */
--arbitrage-warning: #f59e0b;      /* 警告：橙色（中风险） */
--arbitrage-success: #22c55e;      /* 成功：亮绿色 */
--arbitrage-info: #06b6d4;         /* 信息：青色 */

/* 套利类型标识色 */
--type-spot: #8b5cf6;              /* 紫色 - 现货套利 */
--type-triangular: #ec4899;        /* 粉色 - 三角套利 */
--type-funding: #f59e0b;           /* 橙色 - 资金费率 */
--type-basis: #3b82f6;             /* 蓝色 - 期现套利 */
--type-crosschain: #14b8a6;        /* 青绿 - 跨链 */
--type-depeg: #ef4444;             /* 红色 - 脱锚 */
```

### 6.2 图标系统

- 🔄 跨所套利
- 🔺 三角套利
- 💰 资金费率套利
- 📊 期现套利
- 🌉 跨链套利
- ⚖️ 脱锚套利

### 6.3 动效设计

- **新信号出现**：从右侧滑入 + 闪烁提示
- **信号过期**：淡出并标记
- **价格变动**：数字跳动动画
- **收益计算**：进度条动画

---

## 七、开发排期

### Phase 1: 基础架构（Week 1-2）
- [ ] 数据库设计和创建
- [ ] API 架构搭建
- [ ] 前端页面结构
- [ ] WebSocket 服务

### Phase 2: 核心功能（Week 3-4）
- [ ] 信号检测引擎（跨所套利）
- [ ] 信号列表页面
- [ ] 信号详情页面
- [ ] 基础筛选功能

### Phase 3: 高级功能（Week 5-6）
- [ ] 其他套利类型检测
- [ ] 实时推送功能
- [ ] 提醒系统
- [ ] 监控面板

### Phase 4: 优化与测试（Week 7-8）
- [ ] 性能优化
- [ ] 会员权限控制
- [ ] 全面测试
- [ ] 文档完善

---

## 八、关键技术挑战

### 8.1 实时性保证
- **挑战**：套利窗口短暂，延迟必须控制在 3 秒内
- **方案**：WebSocket + Redis Pub/Sub + 分布式部署

### 8.2 数据准确性
- **挑战**：交易所 API 数据可能延迟或不准确
- **方案**：多数据源交叉验证 + 异常检测

### 8.3 高并发处理
- **挑战**：大量用户同时订阅实时信号
- **方案**：负载均衡 + Redis 集群 + CDN

### 8.4 跨所套利的复杂性
- **挑战**：需要考虑提现时间、限额、手续费变化
- **方案**：维护交易所配置数据库 + 动态计算

---

## 九、成功指标（KPI）

| 指标 | 目标值 | 衡量周期 |
|------|--------|----------|
| 信号准确率 | > 85% | 月度 |
| 信号延迟 | < 3 秒 | 实时监控 |
| 月活跃用户 | 5000+ | 月度 |
| 会员转化率 | > 15% | 月度 |
| 用户平均停留时间 | > 8 分钟 | 周度 |
| 每用户查看信号数 | > 20 个/天 | 日度 |
| Telegram Bot 订阅数 | 2000+ | 月度 |

---

## 十、风险与合规

### 10.1 法律风险
- ⚠️ **免责声明**：所有信号仅供参考，不构成投资建议
- ⚠️ **地域限制**：遵守各国法律，限制特定地区访问
- ⚠️ **用户协议**：明确平台不承担交易损失

### 10.2 技术风险
- ⚠️ **API 限流**：交易所 API 可能限制访问频率
- ⚠️ **数据延迟**：网络问题导致数据不及时
- ⚠️ **系统故障**：服务器宕机或 Bug

### 10.3 缓解措施
- 📜 完善的免责条款
- 🔒 严格的数据验证
- 📊 实时监控和告警
- 💾 数据备份和灾备

---

## 十一、附录

### 11.1 参考资源

**类似产品**：
- Coindash（套利监控）
- ArbitrageScanner
- CryptoArbitrage

**技术文档**：
- Binance API: https://binance-docs.github.io/apidocs/
- OKX API: https://www.okx.com/docs-v5/
- WebSocket 最佳实践

### 11.2 术语表

| 术语 | 解释 |
|------|------|
| Spread | 价差，买入价与卖出价之间的差异 |
| Slippage | 滑点，实际成交价与预期价格的偏差 |
| Liquidity | 流动性，可交易的资产数量 |
| Funding Rate | 资金费率，永续合约的持仓费用 |
| Basis | 基差，现货价格与期货价格的差异 |
| Depeg | 脱锚，稳定币偏离其锚定价格 |

---

**文档版本控制**：
- v1.0 - 2025-01-10 - 初始版本
- v1.1 - TBD - 根据开发反馈更新

**负责人**：
- 产品：PlayNew Team
- 技术：TBD
- 设计：TBD

**下一步行动**：
1. 技术评审（评估技术可行性）
2. 设计评审（UI/UX 设计稿）
3. 资源分配（开发人员、时间表）
4. 开始 Phase 1 开发

---

*文档结束*