# 🆓 免费八卦采集系统设置指南

## 📋 你现在有的选项

| 工作流 | 成本 | 配置难度 | 推荐指数 |
|--------|------|----------|----------|
| **RSS 聚合采集器** | $0 | ⭐ 简单 | ⭐⭐⭐⭐⭐ |
| **Telegram 监控** | $0 | ⭐⭐ 中等 | ⭐⭐⭐⭐ |

---

## 🏆 方案 1: RSS 聚合采集器（最推荐）

### 为什么选择 RSS？

✅ **完全免费** - 无需任何 API Key
✅ **配置简单** - 5分钟即可运行
✅ **合法合规** - 使用公开 RSS Feed
✅ **稳定可靠** - 不会被封禁
✅ **覆盖广泛** - 10+ 主流媒体

### 数据源覆盖

#### 中文媒体（5个）
- 吴说区块链
- 律动 BlockBeats
- PANews
- 8BTC
- 金色财经

#### 英文媒体（5个）
- Cointelegraph
- CoinDesk
- The Block
- Decrypt
- CryptoSlate

### 快速开始

#### 步骤 1: 导入工作流（1分钟）

1. 打开 n8n：`http://localhost:5678`
2. 点击 **"+" → "Import from File"**
3. 选择：**`rss-gossip-collector.json`**
4. 导入成功！

#### 步骤 2: 配置环境变量（1分钟）

```bash
# 编辑 .env 文件
DIRECTUS_URL=http://host.docker.internal:8055
DIRECTUS_TOKEN=你的_directus_token
```

**获取 Token:**
```bash
curl -X POST http://localhost:8055/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"the_uk1@outlook.com","password":"Mygcdjmyxzg2026!"}' \
  | jq -r '.data.access_token'
```

**重启 n8n 以加载环境变量:**
```bash
docker-compose -f docker-compose.n8n.yml restart
```

#### 步骤 3: 测试运行（1分钟）

1. 禁用 "Schedule Trigger" 节点（首次测试）
2. 点击右上角 **"Execute Workflow"**
3. 观察结果：
   - "Generate RSS Sources" 应该输出 10 个 RSS 源
   - "RSS Read Feed" 应该获取到新闻列表
   - "Filter & Score Gossip" 应该筛选出八卦内容
   - "Publish to Directus" 应该成功发布

#### 步骤 4: 启用自动运行（30秒）

1. 启用 "Schedule Trigger" 节点
2. 保存工作流（Ctrl+S）
3. 工作流将每 30 分钟自动运行

### 查看结果

```bash
# 查看采集到的八卦
curl -s 'http://localhost:8055/items/news?filter[source_type][_eq]=rss&filter[news_type][_eq]=gossip&sort=-published_at&limit=10&fields=id,title,source,credibility_score' | jq

# 或访问前端
open http://localhost:3000/gossip
```

### 工作流说明

```
Schedule Trigger (每30分钟)
    ↓
Generate RSS Sources (生成10个RSS源)
    ↓
RSS Read Feed (读取每个源的最新文章)
    ↓
Filter & Score Gossip (关键词过滤 + 评分)
    ↓
Check Duplicate (去重检查)
    ↓
Publish to Directus (发布到CMS)
    ↓
Summary Stats (统计汇总)
```

### 关键词库

工作流使用以下关键词识别八卦：

**强八卦词**（权重高，降低可信度）:
- 中文: 传闻、爆料、据悉、跑路、卷款、暴雷、被捕、调查
- 英文: rumor, allegedly, scam, rug pull, arrested, hack

**弱八卦词**（权重低）:
- 中文: 争议、质疑、批评
- 英文: controversy, criticism, concern

### 预期产出

- **每次运行**: 从 100-200 条新闻中筛选出 5-15 条八卦
- **每天**: 约 20-50 条八卦（每30分钟运行一次）
- **可信度**: 60-85 分（RSS 来源相对可信）

### 优势

1. ✅ **无需 API Key** - 完全免费
2. ✅ **稳定可靠** - RSS 是标准协议
3. ✅ **主流媒体** - 覆盖币圈主要新闻源
4. ✅ **自动评分** - 基于来源和内容质量
5. ✅ **自动分类** - 项目传闻、交易所、安全事件等

### 劣势

1. ❌ **时效性稍差** - 30分钟延迟（可调整为15分钟）
2. ❌ **不包含社交媒体** - 无法获取 Twitter/X 实时爆料
3. ❌ **需要关键词匹配** - 可能遗漏部分八卦

---

## 🏆 方案 2: Telegram 频道监控（实时性强）

### 为什么选择 Telegram？

✅ **完全免费** - Telegram Bot API 永久免费
✅ **实时性强** - 消息推送，秒级响应
✅ **数据质量高** - 很多 KOL 主要在 TG 活跃
✅ **覆盖面广** - 可监控多个频道/群组

### 快速开始

#### 步骤 1: 创建 Telegram Bot（5分钟）

1. 在 Telegram 中搜索 **@BotFather**
2. 发送 `/newbot`
3. 按提示设置 bot 名称和用户名
4. 获得 **Bot Token**（类似 `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`）
5. 保存这个 Token

#### 步骤 2: 添加 Bot 到频道（3分钟）

**公开频道**:
1. 打开你想监控的 Telegram 频道
2. 频道设置 → 管理员 → 添加管理员
3. 搜索你的 bot 用户名，添加为管理员
4. 给予"发布消息"权限即可

**私有群组**:
1. 打开群组
2. 群组设置 → 添加成员
3. 搜索你的 bot 用户名，添加
4. Bot 需要读取消息权限

#### 推荐监控的频道

**中文频道**（举例，需要自己搜索添加）:
- 吴说区块链
- 币圈日报
- 链捕手
- 深潮 TechFlow

**英文频道**:
- Crypto News
- DeFi News
- NFT Signals

#### 步骤 3: 导入工作流（1分钟）

1. 打开 n8n：`http://localhost:5678`
2. 点击 **"+" → "Import from File"**
3. 选择：**`telegram-gossip-collector.json`**

#### 步骤 4: 配置 Telegram 凭证（2分钟）

1. 点击 **"Telegram Trigger"** 节点
2. 在 "Credential to connect with" 选择 **"Create New Credential"**
3. 选择 **"Telegram API"**
4. 填写：
   - **Name**: Telegram Gossip Bot
   - **Access Token**: 粘贴你从 BotFather 获得的 Token
5. 点击 **"Save"**

#### 步骤 5: 激活工作流（30秒）

1. 点击右上角的 **"Inactive"** 切换为 **"Active"**
2. 保存工作流（Ctrl+S）
3. 工作流现在处于监听状态

#### 步骤 6: 测试（1分钟）

1. 在你添加了 bot 的 Telegram 频道中发送一条测试消息：
   ```
   测试：据悉某项目传闻跑路
   ```

2. 回到 n8n，点击左侧 **"Executions"**
3. 应该看到一条新的执行记录
4. 点击查看详情，检查是否成功发布到 Directus

### 工作流说明

```
Telegram Trigger (实时监听)
    ↓
Parse & Filter Message (解析消息 + 关键词过滤)
    ↓
Publish to Directus (发布到CMS)
    ↓
Log Result (记录结果)
```

### 自动过滤规则

工作流会自动：
1. ✅ 检测八卦关键词（传闻、爆料、跑路、hack 等）
2. ✅ 计算可信度评分（频道 > 群组，长消息 > 短消息）
3. ✅ 识别是否包含图片、视频、链接
4. ✅ 记录消息来源和作者
5. ✅ 自动发布（可信度 ≥60）或保存为草稿（<60）

### 预期产出

- **实时性**: 秒级响应
- **每天**: 取决于你监控的频道活跃度
- **可信度**: 55-75 分（Telegram 可信度中等）

### 优势

1. ✅ **实时响应** - 消息推送，无延迟
2. ✅ **覆盖 KOL** - 很多 KOL 主要在 TG 活跃
3. ✅ **支持媒体** - 图片、视频、文件
4. ✅ **交互性** - 可以回复消息（可扩展功能）

### 劣势

1. ❌ **需要手动添加频道** - 每个频道都要加 bot
2. ❌ **部分频道不允许 bot** - 私密群组可能拒绝
3. ❌ **需要持续运行** - n8n 必须保持在线

---

## 🚀 推荐组合使用

### 最佳实践

**同时运行两个工作流**:
1. **RSS 采集器** - 覆盖主流媒体，每 30 分钟运行
2. **Telegram 监控** - 实时监听频道，补充社交媒体

这样可以：
- ✅ 覆盖媒体 + 社交媒体
- ✅ 定时采集 + 实时推送
- ✅ 高可信度（RSS）+ 独家爆料（TG）

### 成本对比

| 方案 | 月成本 | 日产出 | 覆盖面 |
|------|--------|--------|--------|
| 仅 RSS | **$0** | 20-50 条 | 媒体 |
| 仅 Telegram | **$0** | 10-30 条 | 社交 |
| RSS + Telegram | **$0** | 30-80 条 | 全面 ✅ |
| Twitter API | $100+ | 30-80 条 | 社交 |

**结论**: RSS + Telegram 组合是**完全免费**且效果最好的方案！

---

## 🔧 进阶配置

### 添加更多 RSS 源

编辑 "Generate RSS Sources" 节点，添加：

```javascript
// 添加更多 RSS 源
{ name: '币安博客', url: 'https://www.binance.com/en/blog/rss.xml', weight: 85, lang: 'en' },
{ name: 'CryptoCompare', url: 'https://www.cryptocompare.com/feed/', weight: 75, lang: 'en' },
```

### 调整关键词库

编辑 "Filter & Score Gossip" 节点：

```javascript
// 添加自定义关键词
const gossipKeywords = {
  strong: [
    '传闻', '爆料', '跑路',
    // 添加你的关键词
    '你的关键词1', '你的关键词2'
  ],
  weak: ['争议', '质疑']
};
```

### 调整采集频率

编辑 "Schedule Trigger" 节点：
- 每 15 分钟: `minutesInterval: 15`
- 每 1 小时: 改为 `hoursInterval: 1`

### 调整可信度阈值

编辑 "Publish to Directus" 节点的 `status` 字段：

```javascript
// 原本: credibilityScore >= 60 发布
// 提高标准: credibilityScore >= 70 发布
"status": "{{ $json.credibilityScore >= 70 ? 'published' : 'draft' }}"
```

---

## 📊 监控与统计

### 查看工作流执行历史

n8n 界面 → 左侧 **"Executions"**

可以看到：
- ✅ 每次执行的时间
- ✅ 处理的数据量
- ✅ 成功/失败状态
- ✅ 详细日志

### 查看采集统计

```bash
# RSS 采集统计
curl -s 'http://localhost:8055/items/news?filter[source_type][_eq]=rss&aggregate[count]=id' | jq

# Telegram 采集统计
curl -s 'http://localhost:8055/items/news?filter[source_type][_eq]=telegram&aggregate[count]=id' | jq

# 今日新增八卦
curl -s 'http://localhost:8055/items/news?filter[news_type][_eq]=gossip&filter[published_at][_gte]='$(date -u +%Y-%m-%dT00:00:00Z)'&aggregate[count]=id' | jq
```

---

## 🐛 故障排查

### RSS 工作流报错

**问题**: "Error fetching RSS feed"

**解决方法**:
1. 检查网络连接
2. 某些 RSS 源可能暂时不可用，这是正常的
3. 工作流会继续处理其他源

### Telegram 工作流无反应

**问题**: 发送消息后，n8n 没有执行

**解决方法**:
1. 确保工作流处于 **"Active"** 状态
2. 检查 bot 是否已添加到频道并有权限
3. 查看 n8n 日志：`docker logs playnew-n8n -f`

### Directus 发布失败

**问题**: 401 Unauthorized

**解决方法**:
```bash
# 重新获取 Token
curl -X POST http://localhost:8055/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"the_uk1@outlook.com","password":"Mygcdjmyxzg2026!"}' \
  | jq -r '.data.access_token'

# 更新环境变量
# 编辑 .env 文件，更新 DIRECTUS_TOKEN
# 重启 n8n
docker-compose -f docker-compose.n8n.yml restart
```

---

## 🎉 总结

你现在有了**两个完全免费**的八卦采集方案：

1. **RSS 聚合采集器** - 每 30 分钟从 10+ 媒体采集
2. **Telegram 监控** - 实时监听频道消息

**建议**:
- 先启用 RSS 采集器（最简单）
- 有时间再配置 Telegram 监控（更全面）
- 两个同时运行效果最好！

**预期效果**:
- 每天采集 30-80 条币圈八卦
- 覆盖主流媒体 + 社交媒体
- 自动评分、分类、发布
- **总成本: $0/月** 🎉

开始使用吧！🚀
